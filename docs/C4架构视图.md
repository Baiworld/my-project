# C4 架构视图 — 冷启动音乐与视频混合推荐系统

> 使用 [Mermaid](https://mermaid.js.org/syntax/c4.html) C4 建模语言绘制  
> 覆盖四个抽象层级：System Context → Container → Component → Code  
> 版本：V1.2 / 日期：2026-06-13

---

## 1. C4 Level 1 — 系统上下文视图 (System Context)

```mermaid
C4Context
    title 系统上下文视图 — 冷启动音乐与视频混合推荐系统

    Person(end_user, "普通用户", "消费音乐/视频推荐内容，产生行为数据")
    Person(operator, "运营人员", "监控数据大屏，查询分析结果，调整推荐策略")
    Person(admin, "系统管理员", "管理用户账号、角色权限、系统参数配置")

    System_Ext(upstream_cms, "上游业务系统", "采集用户行为埋点与内容元数据，写入 Kafka")
    System_Ext(email_srv, "邮件服务器", "发送注册验证邮件与系统通知")

    System(hybrid_rec_sys, "冷启动音乐与视频混合推荐系统", "实时消费 Kafka 数据 → 实时/离线分析 → 混合推荐 → 数据可视化")

    Rel(end_user, hybrid_rec_sys, "获取推荐内容，上报行为", "HTTPS/REST")
    Rel(operator, hybrid_rec_sys, "查看大屏，查询数据，导出报表", "HTTPS/WebSocket")
    Rel(admin, hybrid_rec_sys, "管理用户与权限，配置系统参数", "HTTPS/REST")
    Rel(upstream_cms, hybrid_rec_sys, "推送用户行为、内容元数据、注册事件", "Kafka TCP")
    Rel(hybrid_rec_sys, email_srv, "发送验证邮件", "SMTP")
```

### 元素说明

| 元素 | 类型 | 说明 |
|------|------|------|
| 普通用户 | Person | 平台终端用户，消费推荐内容（音乐+视频），产生播放、点赞、收藏等行为 |
| 运营人员 | Person | 查看数据大屏实时监控，条件查询 MySQL 分析结果，导出数据报表 |
| 系统管理员 | Person | 用户账号 CRUD、角色分配、系统参数配置、模型版本管理 |
| 上游业务系统 | External System | 负责采集用户行为埋点与内容元数据，以 JSON 格式写入 Kafka 三个 Topic |
| 邮件服务器 | External System | 发送账户注册验证邮件，系统通知邮件 |
| 混合推荐系统 | System | 本项目的核心系统，覆盖数据接入→实时分析→离线推荐→后端服务→前端展示全链路 |

---

## 2. C4 Level 2 — 容器视图 (Container View)

```mermaid
C4Container
    title 容器视图 — 冷启动音乐与视频混合推荐系统

    Person(end_user, "普通用户", "消费推荐内容")
    Person(operator, "运营人员", "监控与查询")
    Person(admin, "系统管理员", "管理与配置")

    System_Ext(upstream_cms, "上游业务系统", "行为数据与内容元数据")
    System_Ext(email_srv, "邮件服务器", "SMTP")

    System_Boundary(hybrid_rec_sys, "混合推荐系统") {

        Container(kafka, "Kafka 消息队列", "Kafka 3.x", "中转用户行为、内容元数据、注册事件三个 Topic，解耦数据生产与消费")

        Container(spark_streaming, "Spark Streaming 实时分析", "Scala 2.13 + Spark 3.3.1", "消费 Kafka 数据进行实时聚合：用户画像、内容热度、冷启动聚类，结果写入 MySQL 实时表")

        Container(spark_offline, "SparkSQL 离线分析", "Scala 2.13 + Spark 3.3.1", "从 MySQL 实时表读取数据，执行离线画像计算、混合推荐、效果指标计算，结果写回 MySQL 离线表")

        Container(mysql, "MySQL 数据库", "MySQL 5.7", "存储实时分析结果、离线分析结果、系统管理数据")

        Container(flask, "Flask API 服务", "Python 3.11 + Flask", "提供 REST API（认证、权限、数据查询、导出）和 WebSocket 推送（大屏实时数据）")

        Container(vue, "Vue SPA 前端", "Vue 3 + ECharts + Axios", "单页应用：登录注册、数据大屏、数据查询、系统管理页面")
    }

    Rel(end_user, vue, "浏览推荐内容，产生行为", "HTTPS")
    Rel(operator, vue, "查看大屏与查询数据", "HTTPS + WSS")
    Rel(admin, vue, "管理用户与系统配置", "HTTPS")
    Rel(upstream_cms, kafka, "推送行为/元数据/注册事件", "Kafka TCP")
    
    Rel(vue, flask, "API 调用与 WebSocket 连接", "HTTPS + WSS")
    Rel(flask, mysql, "读写用户数据、查询分析结果、系统管理", "JDBC/TCP")
    Rel(flask, email_srv, "发送验证邮件", "SMTP")
    
    Rel(kafka, spark_streaming, "消费消息", "Kafka TCP")
    Rel(spark_streaming, mysql, "写入实时分析结果", "JDBC/TCP")
    Rel(mysql, spark_offline, "读取实时表，写入离线表", "JDBC/TCP")
```

### 容器职责

| 容器 | 技术栈 | 核心职责 |
|------|--------|---------|
| Kafka | Kafka 3.x | 消息中间件，承载 3 个 Topic（user_behavior, content_metadata, user_register），解耦上游与 Spark 消费端 |
| Spark Streaming | Scala 2.13.10 + Spark 3.3.1 | 实时消费 Kafka，5 分钟窗口聚合用户画像，1 分钟窗口统计内容热度，实时 K-Means 冷启动聚类，内容元数据持久化 |
| SparkSQL 离线 | Scala 2.13.10 + Spark 3.3.1 | 全量画像更新，ALS 模型训练（隐式反馈），混合推荐（三路分流+DPP+内容特征回退），每日指标统计 |
| MySQL | MySQL 5.7 | 核心存储：实时表 3 张（rt_*）、离线表 3 张（offline_*）、系统表 4 张（sys_*）、内容元数据表（content_metadata），共 11 张表 |
| Flask API | Python 3.11 + Flask | 认证/授权/查询/导出 REST API，WebSocket 实时推送 |
| Vue SPA | Vue 3 + ECharts + Axios | 前端单页应用，ECharts 数据大屏（7 图表 + 热力图），条件查询表单，管理系统界面 |

---

## 3. C4 Level 3 — 组件视图 (Component View)

### 3.1 Kafka 消息队列 — 组件视图

```mermaid
C4Component
    title 组件视图 — Kafka 消息队列

    Container_Boundary(kafka_container, "Kafka 消息队列") {
        Component(topic_behavior, "user_behavior Topic", "3 分区", "用户行为日志：播放/点赞/收藏/评论/跳过/完播/分享")
        Component(topic_metadata, "content_metadata Topic", "2 分区", "内容元数据：音乐特征/视频特征/标签/时长")
        Component(topic_register, "user_register Topic", "2 分区", "用户注册事件：设备信息/注册渠道/初始兴趣标签")
        Component(dlq, "Dead Letter Queue", "1 分区", "数据校验失败的消息暂存，供后续人工排查")
    }

    Container(spark_streaming, "Spark Streaming", "Scala", "消费 Kafka 并实时分析")
    Container_Ext(upstream, "上游业务系统", "数据生产者")

    Rel(upstream, topic_behavior, "写入用户行为 JSON", "Kafka Producer API")
    Rel(upstream, topic_metadata, "写入内容元数据 JSON", "Kafka Producer API")
    Rel(upstream, topic_register, "写入注册事件 JSON", "Kafka Producer API")
    Rel(topic_behavior, spark_streaming, "消费", "Spark Kafka Consumer")
    Rel(topic_metadata, spark_streaming, "消费", "Spark Kafka Consumer")
    Rel(topic_register, spark_streaming, "消费", "Spark Kafka Consumer")
    Rel(spark_streaming, dlq, "校验失败 → 死信", "Kafka Producer API")
```

### 3.2 Spark Streaming 实时分析 — 组件视图

```mermaid
C4Component
    title 组件视图 — Spark Streaming 实时分析

    Container_Boundary(spark_streaming_container, "Spark Streaming 实时分析 (Scala)") {
        Component(validator, "DataValidator", "数据校验与去重", "字段完整性校验；user_id+content_id+event_type 在 60s 窗口内去重；异常数据打入 DLQ")
        Component(profile_agg, "UserProfileAgg", "用户画像聚合", "5 分钟滑动窗口聚合：播放次数/完播率/点赞率/收藏率/跳过率/偏好分布/活跃时段；行为数 ≤50 标记为冷启动用户")
        Component(hot_stats, "ContentHotStats", "内容热度统计", "1 分钟滚动窗口统计播放量/点赞/收藏/分享；计算加权热度分 hot_score；区分音乐/视频类型")
        Component(coldstart_cluster, "ColdStartCluster", "冷启动聚类", "基于注册信息（设备/渠道/标签）实时 K-Means 聚类；K=8（可配）；结果写入 rt_coldstart_cluster")
        Component(mysql_writer, "MySQLBatchWriter", "批量写入 MySQL", "批量 INSERT（batch size=500）；写入 rt_user_profile / rt_content_hot / rt_coldstart_cluster / content_metadata")
    }

    Container_Ext(kafka, "Kafka", "数据源")
    Container_Ext(mysql, "MySQL", "存储目标")
    Container_Ext(dlq, "Dead Letter Queue", "异常消息")

    Rel(kafka, validator, "消费 DStream", "Spark Kafka Consumer")
    Rel(validator, dlq, "校验失败写入", "Kafka Producer")
    Rel(validator, profile_agg, "过滤后的 user_behavior 流", "DStream transform")
    Rel(validator, hot_stats, "过滤后的 user_behavior 流", "DStream transform")
    Rel(validator, coldstart_cluster, "过滤后的 user_register 流", "DStream transform")
    Rel(profile_agg, mysql_writer, "用户画像聚合结果", "foreachRDD")
    Rel(hot_stats, mysql_writer, "内容热度统计结果", "foreachRDD")
    Rel(coldstart_cluster, mysql_writer, "聚类结果", "foreachRDD")
    Rel(mysql_writer, mysql, "写入 rt_user_profile / rt_content_hot / rt_coldstart_cluster", "JDBC batch insert")
```

### 3.3 SparkSQL 离线分析 — 组件视图

```mermaid
C4Component
    title 组件视图 — SparkSQL 离线分析

    Container_Boundary(spark_offline_container, "SparkSQL 离线分析 (Scala)") {
        Component(portrait_builder, "UserPortraitBuilder", "用户全量画像", "从 rt_user_profile 全量读取 → 聚合长期兴趣标签 → 用户生命周期阶段判定 → 128 维偏好向量输出")
        Component(als_module, "ALSTrainer + ALSRecommender", "ALS 协同过滤", "从 rt_user_profile 读取真实行为数据构建隐式反馈矩阵 → ALS 训练 (rank=50) → 批量推理")
        Component(hybrid_recommender, "HybridRecommender", "混合推荐引擎", "三路分流：冷启动（聚类热门+标签匹配+DPP）+ 过渡期（线性插值）+ 存量（ALS+内容特征回退）；音乐:视频=6:4")
        Component(metrics_calc, "MetricsCalculator", "推荐效果指标", "每日计算 CTR/CVR/人均播放时长/人均互动/覆盖率/多样性/冷启动转化率；按用户群与内容类型分组")
        Component(mysql_reader, "MySQLSparkReader", "读取 MySQL", "通过 SparkSQL JDBC 读取 MySQL 实时表数据到 DataFrame")
        Component(mysql_writer, "MySQLSparkWriter", "写入 MySQL", "将离线计算结果批量写入 MySQL 离线表")
    }

    Container_Ext(mysql, "MySQL", "数据源 & 存储目标")

    Rel(mysql, mysql_reader, "读取 rt_user_profile / rt_content_hot / rt_coldstart_cluster", "SparkSQL JDBC")
    Rel(mysql_reader, portrait_builder, "用户实时画像 DF", "DataFrame pass")
    Rel(mysql_reader, als_module, "用户行为交互矩阵", "DataFrame pass")
    Rel(mysql_reader, hybrid_recommender, "用户画像 + 聚类 + 热度 DF", "DataFrame pass")
    Rel(portrait_builder, mysql_writer, "全量画像结果", "DataFrame write")
    Rel(als_module, hybrid_recommender, "ALS 模型 + 物品向量", "内部依赖")
    Rel(hybrid_recommender, mysql_writer, "推荐结果列表", "DataFrame write")
    Rel(hybrid_recommender, metrics_calc, "推荐日志 DF", "DataFrame pass")
    Rel(metrics_calc, mysql_writer, "效果指标", "DataFrame write")
    Rel(mysql_writer, mysql, "写入 offline_user_portrait / offline_recommendations / offline_metrics", "JDBC batch insert")
```

### 3.4 Flask API 服务 — 组件视图

```mermaid
C4Component
    title 组件视图 — Flask API 服务

    Container_Boundary(flask_container, "Flask API 服务 (Python 3.11)") {
        Component(auth_ctrl, "AuthController", "认证控制器", "POST /api/auth/register — bcrypt 哈希注册；POST /api/auth/login — JWT 签发（HS256, 2h 有效期）；POST /api/auth/refresh — Refresh Token 续期；POST /api/auth/logout — Token 失效")
        Component(perm_mgr, "PermissionManager", "权限管理器", "RBAC 装饰器 @require_role('admin'/'operator') 拦截请求；基于 sys_user_role 表校验角色；操作审计日志写入 sys_audit_log")
        Component(query_api, "QueryController", "查询控制器", "7 个端点：recommendations / metrics / content+hot / users / coldstart+stats / region / admin；实时优先策略（rt_* 表优先，offline_* 备选）；支持分页/筛选")
        Component(export_svc, "ExportService", "导出服务", "GET /api/export/* — 按时间/用户群/内容类型筛选导出 CSV/Excel；异步生成 + 下载链接")
        Component(ws_server, "WebSocketServer", "WebSocket 推送", "DashboardPusher：每 5 秒推送大屏全量快照（指标/趋势/聚类/策略分布）；EventLogPusher：每 2 秒推送用户行为滚动日志（50条缓存）")
        Component(db_pool, "MySQLConnectionPool", "数据库连接池", "SQLAlchemy + PyMySQL 连接池；min=5, max=20；读写分离路由（实时表→主库，离线表→从库）")
        Component(email_svc, "EmailService", "邮件服务", "注册验证邮件发送；SMTP 连接；验证码 6 位数字，有效期 10 分钟")
    }

    Container_Ext(vue, "Vue SPA", "前端")
    Container_Ext(mysql, "MySQL", "数据库")
    System_Ext(email_srv, "邮件服务器", "SMTP")

    Rel(vue, auth_ctrl, "登录/注册/刷新 Token", "HTTPS JSON")
    Rel(vue, query_api, "数据查询请求", "HTTPS JSON")
    Rel(vue, export_svc, "导出请求", "HTTPS")
    Rel(vue, ws_server, "大屏实时数据订阅", "WSS")
    Rel(auth_ctrl, perm_mgr, "签发带角色的 JWT Payload", "内部调用")
    Rel(perm_mgr, query_api, "拦截校验", "装饰器")
    Rel(perm_mgr, export_svc, "拦截校验", "装饰器")
    Rel(auth_ctrl, db_pool, "用户 CRUD", "SQLAlchemy ORM")
    Rel(query_api, db_pool, "查询 rt_* / offline_*", "SQLAlchemy ORM")
    Rel(export_svc, db_pool, "查询 + 流式导出", "SQLAlchemy ORM")
    Rel(ws_server, db_pool, "轮询最新数据", "SQLAlchemy ORM")
    Rel(db_pool, mysql, "SQL 读写", "PyMySQL/TCP")
    Rel(auth_ctrl, email_svc, "发送验证邮件", "内部调用")
    Rel(email_svc, email_srv, "SMTP 发送", "SMTP/TCP")
```

### 3.5 Vue SPA 前端 — 组件视图

```mermaid
C4Component
    title 组件视图 — Vue SPA 前端

    Container_Boundary(vue_container, "Vue SPA 前端 (Vue 3)") {
        Component(router, "VueRouter", "路由管理", "/login /register — 登录注册；/dashboard — 数据大屏；/query/* — 数据查询；/admin/* — 系统管理；路由守卫校验 Token")
        Component(auth_store, "AuthStore (Pinia)", "状态管理 — 认证", "存储 JWT Token + Refresh Token + 用户角色；自动刷新 Token 拦截器；角色权限前端校验")
        Component(login_page, "LoginRegisterPage", "登录注册页", "用户名/邮箱 + 密码登录；注册表单含密码强度校验；验证码倒计时；FR-14 完整实现")
        Component(dashboard, "DashboardPage", "数据大屏页", "4 标签切换 + 4 指标卡片 + 9 图表（折线/柱状/环形/饼图/漏斗/热力图/对比图/滚动日志）+ WebSocket 5s 推送 + setOption 增量更新；FR-15 完整实现")
        Component(query_page, "QueryPage", "数据查询页", "用户画像查询 + 推荐效果查询 + 热门内容查询；日期范围选择器/类型筛选/Top N 选择；分页表格；FR-16 完整实现")
        Component(admin_page, "AdminPage", "系统管理页", "用户列表 + 角色分配 + 启用/禁用开关 + 操作审计日志表格；仅 admin 角色可见；FR-17 完整实现")
        Component(api_client, "ApiClient (Axios)", "HTTP 客户端", "统一封装 Axios 实例：baseURL / 超时 10s / 请求拦截器注入 JWT / 响应拦截器处理 401 自动刷新或跳转登录页")
        Component(ws_client, "WebSocketClient", "WebSocket 客户端", "管理与 Flask WebSocket 的连接；自动重连（指数退避 max 30s）；心跳保活（25s ping）；dashboard_update + user_event 回调注册")
    }

    Container_Ext(flask, "Flask API", "后端服务")

    Rel(router, login_page, "路由匹配", "Vue Router")
    Rel(router, dashboard, "路由匹配", "Vue Router")
    Rel(router, query_page, "路由匹配", "Vue Router")
    Rel(router, admin_page, "路由匹配", "Vue Router")
    Rel(router, auth_store, "路由守卫校验", "beforeEach")
    Rel(auth_store, api_client, "注入 Token", "Axios interceptor")
    Rel(login_page, api_client, "登录/注册 API", "HTTPS POST")
    Rel(login_page, auth_store, "存储 Token", "Pinia action")
    Rel(dashboard, api_client, "获取指标历史数据", "HTTPS GET")
    Rel(dashboard, ws_client, "订阅实时推送", "WebSocket")
    Rel(query_page, api_client, "条件查询", "HTTPS GET")
    Rel(admin_page, api_client, "用户管理 CRUD", "HTTPS POST/PUT/DELETE")
    Rel(api_client, flask, "REST API 调用", "HTTPS JSON")
    Rel(ws_client, flask, "WebSocket 连接", "WSS")
```

---

## 4. C4 Level 4 — 代码视图 (Code View)

> Mermaid 无原生 C4Code 图类型，使用 **Class Diagram** 按模块展示核心类/对象及其关系。

### 4.1 Spark Streaming 实时分析 — 代码级类图

```mermaid
classDiagram
    title 代码视图 — Spark Streaming 实时分析模块

    %% ========== 入口 ==========
    class RealTimeAnalysisApp {
        +main(args: Array[String]): Unit
        -buildSparkSession(): SparkSession
        -createKafkaStream(ss: SparkSession, topics: Seq[String]): DStream[String]
    }

    %% ========== 数据模型 ==========
    class UserBehaviorEvent {
        +eventId: String
        +userId: Long
        +contentId: Long
        +contentType: String
        +eventType: String
        +eventTime: Timestamp
        +duration: Double
        +deviceType: String
        +channel: String
        +sessionId: String
        -isValid(): Boolean
    }

    class ContentMetaEvent {
        +contentId: Long
        +contentType: String
        +title: String
        +artistOrAuthor: String
        +styleOrCategory: String
        +tags: Array[String]
        +duration: Double
        +language: String
        +bpm: Double
        +resolution: String
        +createTime: Timestamp
    }

    class UserRegisterEvent {
        +userId: Long
        +registerTime: Timestamp
        +deviceType: String
        +osVersion: String
        +registerChannel: String
        +interestTags: Array[String]
        +region: String
    }

    %% ========== 数据校验 ==========
    class DataValidator {
        -requiredFields: Map[String, Seq[String]]
        -seenCache: Cache[(Long,Long,String), Boolean]
        +validateBehavior(json: String): Either[String, UserBehaviorEvent]
        +validateMeta(json: String): Either[String, ContentMetaEvent]
        +validateRegister(json: String): Either[String, UserRegisterEvent]
        +deduplicate(event: UserBehaviorEvent): Boolean
        -checkFields(json: JsValue, fields: Seq[String]): Boolean
        -sendToDLQ(invalidJson: String, reason: String): Unit
    }

    %% ========== 用户画像聚合 ==========
    class UserProfileAgg {
        -windowDuration: Duration
        -slideDuration: Duration
        -coldStartThreshold: Int
        +aggregate(stream: DStream[UserBehaviorEvent]): DStream[UserProfile]
        -extractFeatures(events: Iterable[UserBehaviorEvent]): UserFeatures
        -isColdStart(featureCount: Int): Boolean
        -calcPlayCount(events: Iterable[UserBehaviorEvent]): Int
        -calcCompletionRate(events: Iterable[UserBehaviorEvent]): Double
        -calcLikeRate(events: Iterable[UserBehaviorEvent]): Double
        -calcFavoriteRate(events: Iterable[UserBehaviorEvent]): Double
        -calcSkipRate(events: Iterable[UserBehaviorEvent]): Double
        -calcPreferenceDistribution(events: Iterable[UserBehaviorEvent]): Map[String, Double]
        -calcActiveHours(events: Iterable[UserBehaviorEvent]): Map[Int, Int]
    }

    class UserProfile {
        +userId: Long
        +windowStart: Timestamp
        +windowEnd: Timestamp
        +playCount: Int
        +completionRate: Double
        +likeRate: Double
        +favoriteRate: Double
        +skipRate: Double
        +preferenceDistribution: String
        +activeHours: String
        +isColdStart: Boolean
        +behaviorCount: Int
    }

    %% ========== 内容热度 ==========
    class ContentHotStats {
        -windowDuration: Duration
        -weightPlay: Double
        -weightCompletion: Double
        -weightInteraction: Double
        +computeHotness(stream: DStream[UserBehaviorEvent]): DStream[ContentHot]
        -calcHotScore(playCount: Long, completionRate: Double, interactionRate: Double): Double
    }

    class ContentHot {
        +contentId: Long
        +contentType: String
        +playCount: Long
        +likeCount: Long
        +favoriteCount: Long
        +shareCount: Long
        +completionRate: Double
        +interactionRate: Double
        +hotScore: Double
        +windowStart: Timestamp
        +windowEnd: Timestamp
    }

    %% ========== 冷启动聚类 ==========
    class ColdStartCluster {
        -k: Int
        -maxIterations: Int
        -featureCols: Array[String]
        +cluster(stream: DStream[UserRegisterEvent]): DStream[ClusterResult]
        -encodeFeatures(event: UserRegisterEvent): Vector
        -trainOrUpdateModel(features: RDD[Vector]): KMeansModel
        -predictCluster(model: KMeansModel, features: Vector): Int
    }

    class ClusterResult {
        +userId: Long
        +clusterId: Int
        +clusterCenter: String
        +clusterSize: Int
        +computeTime: Timestamp
    }

    %% ========== MySQL 批量写入 ==========
    class MySQLBatchWriter {
        -batchSize: Int
        -jdbcUrl: String
        -tableNames: Map[String, String]
        +writeUserProfile(rdd: RDD[UserProfile]): Unit
        +writeContentHot(rdd: RDD[ContentHot]): Unit
        +writeClusterResult(rdd: RDD[ClusterResult]): Unit
        -batchInsert[T](rdd: RDD[T], tableName: String, f: T => PreparedStatement): Unit
    }

    %% ========== 关系 ==========
    RealTimeAnalysisApp --> DataValidator : "解析 JSON → 校验"
    RealTimeAnalysisApp --> UserProfileAgg : "校验后流 → 聚合"
    RealTimeAnalysisApp --> ContentHotStats : "校验后流 → 热度"
    RealTimeAnalysisApp --> ColdStartCluster : "校验后流 → 聚类"
    RealTimeAnalysisApp --> MySQLBatchWriter : "结果 → 写入"

    DataValidator ..> UserBehaviorEvent : "产出"
    DataValidator ..> ContentMetaEvent : "产出"
    DataValidator ..> UserRegisterEvent : "产出"
    UserProfileAgg ..> UserProfile : "产出"
    ContentHotStats ..> ContentHot : "产出"
    ColdStartCluster ..> ClusterResult : "产出"

    MySQLBatchWriter ..> UserProfile : "写入"
    MySQLBatchWriter ..> ContentHot : "写入"
    MySQLBatchWriter ..> ClusterResult : "写入"

    UserProfileAgg --> DataValidator : "消费 validatedBehaviorStream"
    ContentHotStats --> DataValidator : "消费 validatedBehaviorStream"
    ColdStartCluster --> DataValidator : "消费 validatedRegisterStream"
```

### 4.2 Flask API 服务 — 代码级类图

```mermaid
classDiagram
    title 代码视图 — Flask API 服务

    %% ========== 工厂 ==========
    class ApplicationFactory {
        +create_app(config_name: str): Flask
        -register_extensions(app: Flask): void
        -register_blueprints(app: Flask): void
        -register_error_handlers(app: Flask): void
    }

    %% ========== 认证 ==========
    class AuthBlueprint {
        <<blueprint>>
        +POST /api/auth/register
        +POST /api/auth/login
        +POST /api/auth/refresh
        +POST /api/auth/logout
        +GET /api/auth/verify-email
    }

    class AuthService {
        +register(username: str, email: str, password: str): User
        +login(account: str, password: str): TokenPair
        +refresh_token(refresh_token: str): TokenPair
        +logout(user_id: int): void
        +verify_email(token: str): bool
        -hash_password(password: str): str
        -verify_password(password: str, hashed: str): bool
        -generate_jwt(user: User): str
        -generate_refresh_token(user_id: int): str
    }

    class TokenPair {
        +access_token: str
        +refresh_token: str
        +token_type: str
        +expires_in: int
    }

    %% ========== 权限 ==========
    class PermissionManager {
        +require_role(*roles: str): Callable
        +require_permission(*permissions: str): Callable
        +get_user_roles(user_id: int): List[str]
        +has_role(user_id: int, role: str): bool
        +assign_role(admin_id: int, user_id: int, role: str): void
        +revoke_role(admin_id: int, user_id: int, role: str): void
        -audit_log(operator_id: int, action: str, target: str): void
    }

    %% ========== 查询 ==========
    class QueryBlueprint {
        <<blueprint>>
        +GET /api/recommendations
        +GET /api/metrics
        +GET /api/content/hot
        +GET /api/users/profile
        +GET /api/coldstart/analysis
    }

    class QueryService {
        +get_recommendations(user_id: int, page: int, size: int): PageResult
        +get_metrics(start: str, end: str, group_by: str): List[Metric]
        +get_hot_content(content_type: str, top_n: int): List[ContentHot]
        +get_user_profile(user_id: int): UserProfile
        +get_coldstart_analysis(): ColdStartAnalysis
        -apply_filters(query: Query, filters: Dict): Query
        -paginate(query: Query, page: int, size: int): PageResult
    }

    %% ========== 导出 ==========
    class ExportBlueprint {
        <<blueprint>>
        +GET /api/export/metrics
        +GET /api/export/recommendations
        +GET /api/export/users
    }

    class ExportService {
        +export_metrics(start: str, end: str, format: str): str
        +export_recommendations(user_id: int, format: str): str
        +export_users(filters: Dict, format: str): str
        -to_csv(data: DataFrame, filepath: str): str
        -to_excel(data: DataFrame, filepath: str): str
        -async_generate(task_id: str, func: Callable, *args): void
    }

    %% ========== WebSocket ==========
    class WebSocketBlueprint {
        <<blueprint>>
        +ws /ws/dashboard
        +ws /ws/events
    }

    class DashboardPusher {
        -connections: Set[WebSocket]
        -interval: int
        +register(ws: WebSocket): void
        +unregister(ws: WebSocket): void
        +start_push_loop(): void
        -collect_latest_metrics(): Dict
        -broadcast(payload: str): void
    }

    class EventLogPusher {
        -connections: Set[WebSocket]
        +register(ws: WebSocket): void
        +unregister(ws: WebSocket): void
        +push_event(event: UserBehaviorEvent): void
    }

    %% ========== 邮件 ==========
    class EmailService {
        -smtp_host: str
        -smtp_port: int
        -sender: str
        +send_verification(to: str, code: str): void
        +send_notification(to: str, subject: str, body: str): void
        -build_verification_email(code: str): MIMEText
        -connect(): SMTP
    }

    %% ========== 数据模型 ==========
    class User {
        +id: int
        +username: str
        +email: str
        +password_hash: str
        +is_active: bool
        +is_verified: bool
        +created_at: datetime
        +updated_at: datetime
    }

    class Role {
        +id: int
        +name: str
        +description: str
    }

    class AuditLog {
        +id: int
        +operator_id: int
        +action: str
        +target: str
        +detail: str
        +created_at: datetime
    }

    %% ========== 关系 ==========
    ApplicationFactory --> AuthBlueprint : "注册"
    ApplicationFactory --> QueryBlueprint : "注册"
    ApplicationFactory --> ExportBlueprint : "注册"
    ApplicationFactory --> WebSocketBlueprint : "注册"
    ApplicationFactory --> PermissionManager : "初始化"

    AuthBlueprint --> AuthService : "调用"
    AuthBlueprint --> EmailService : "发验证邮件"
    AuthService ..> TokenPair : "返回"
    AuthService --> User : "操作"
    AuthService --> PermissionManager : "记录审计日志"

    QueryBlueprint --> PermissionManager : "@require_role 拦截"
    QueryBlueprint --> QueryService : "调用"

    ExportBlueprint --> PermissionManager : "@require_role 拦截"
    ExportBlueprint --> ExportService : "调用"

    WebSocketBlueprint --> DashboardPusher : "路由"
    WebSocketBlueprint --> EventLogPusher : "路由"

    DashboardPusher --> QueryService : "查询最新指标"
    EventLogPusher --> QueryService : "查询最新事件"

    PermissionManager --> AuditLog : "写入"
    PermissionManager --> User : "查询角色"
    PermissionManager --> Role : "查询角色"

    QueryService --> User : "关联查询"
    ExportService --> QueryService : "复用查询逻辑"
```

---

## 附录：视图索引

| 视图 | 层级 | 关注点 | 目标读者 |
|------|------|--------|---------|
| 系统上下文视图 | Level 1 | 系统与外部的交互边界 | 全体干系人 |
| 容器视图 | Level 2 | 技术选型与容器间通信 | 架构师、技术负责人 |
| Kafka 组件视图 | Level 3 | 消息队列内部组件与数据流 | 数据工程师 |
| Spark Streaming 组件视图 | Level 3 | 实时分析组件职责与管道 | 数据工程师 |
| SparkSQL 组件视图 | Level 3 | 离线分析组件职责与管道 | 数据工程师 |
| Flask 组件视图 | Level 3 | 后端 API 组件职责与路由 | 后端开发 |
| Vue 组件视图 | Level 3 | 前端页面组件与通信 | 前端开发 |
| Spark Streaming 代码视图 | Level 4 | 实时分析类结构与关系 | 开发工程师 |
| Flask 代码视图 | Level 4 | 后端类结构与关系 | 开发工程师 |
