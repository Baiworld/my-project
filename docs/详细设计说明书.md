# 📘 冷启动音乐与视频混合推荐系统 — 详细设计说明书

> **版本**：V1.1　　　**日期**：2026-06-09　　　**作者**：架构组  
> **技术栈**：`Scala 2.13.10` `Spark 3.3.1` `Python 3.11` `Kafka 3.3.1` `MySQL 5.7` `Flask 3.1` `Vue 3.4`

---

## 目录

1. [引言](#1-引言)
2. [总体架构设计](#2-总体架构设计)
3. [数据流详细设计](#3-数据流详细设计)
4. [🗄️ 数据库详细设计](#4-数据库详细设计)
5. [📨 Kafka 消息队列详细设计](#5-kafka-消息队列详细设计)
6. [⚡ Spark Streaming 实时分析详细设计](#6-spark-streaming-实时分析详细设计)
7. [🧠 SparkSQL 离线分析详细设计](#7-sparksql-离线分析详细设计)
8. [🔧 Flask 后端详细设计](#8-flask-后端详细设计)
9. [🎨 Vue 前端详细设计](#9-vue-前端详细设计)
10. [📊 推荐算法详细设计](#10-推荐算法详细设计)
11. [🔐 安全设计](#11-安全设计)
12. [🚀 部署设计](#12-部署设计)

---

## 1. 引言

### 1.1 文档目的

本文档在《业务需求文档》与《C4 架构视图》基础上，对系统各模块进行**详细设计**，细化到**类/方法/接口/字段/算法伪代码**级别，作为开发人员编码实施的直接依据。

### 1.2 适用范围

- 后端开发工程师：Flask API 接口实现、数据库 DDL 编写
- 数据工程师：Kafka 配置、Spark Streaming/SparkSQL 作业开发
- 前端开发工程师：Vue 页面组件开发、WebSocket 对接
- 测试工程师：接口测试用例、数据校验规则

### 1.3 阅读导航

| 角色 | 优先阅读章节 | 标注 |
|------|-------------|------|
| 👤 数据工程师 | §5 §6 §7 §10 | 💛 数据计算层 |
| 👤 后端工程师 | §4 §8 §11 | 🔵 服务层 |
| 👤 前端工程师 | §9 | 🟣 展示层 |
| 👑 架构师/PM | §2 §3 §12 | 全局 |

---

## 2. 总体架构设计

### 2.1 分层架构总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        🟣 展示层 (Presentation Layer)                         │
│                      Vue 3 + ECharts + Axios + WebSocket                     │
│   ┌──────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│   │ 🔑 登录   │  │ 📊 数据大屏   │  │ 🔍 数据查询   │  │ ⚙️ 系统管理      │   │
│   └──────────┘  └──────────────┘  └──────────────┘  └──────────────────┘   │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │  HTTPS / WSS
┌───────────────────────────────────▼─────────────────────────────────────────┐
│                          🔵 服务层 (Service Layer)                            │
│                        Python 3.12 + Flask + SQLAlchemy                      │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐   │
│   │ 🔐 认证   │  │ 🛡️ 权限   │  │ 📡 查询   │  │ 📥 导出   │  │ 📡 WS推送   │   │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘  └────────────┘   │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │  PyMySQL / TCP
┌───────────────────────────────────▼─────────────────────────────────────────┐
│                          🟢 存储层 (Storage Layer)                            │
│                            MySQL 8.x (主从架构)                              │
│   ┌────────────────┐  ┌──────────────────┐  ┌──────────────────────────┐   │
│   │ ⚡ 实时表 rt_*   │  │ 🧠 离线表 offline_* │  │ 🔧 系统表 sys_*           │   │
│   └────────────────┘  └──────────────────┘  └──────────────────────────┘   │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │  JDBC 写入
┌───────────────────────────────────▼─────────────────────────────────────────┐
│                        💛 计算层 (Compute Layer)                              │
│                          Scala 2.13 + Spark 3.3.1                            │
│   ┌────────────────────────────┐  ┌────────────────────────────────────┐   │
│   │ ⚡ Spark Streaming          │  │ 🧠 SparkSQL 离线批处理               │   │
│   │ · DataValidator            │  │ · UserPortraitBuilder              │   │
│   │ · UserProfileAgg           │  │ · SimilarityMatrix                 │   │
│   │ · ContentHotStats          │  │ · HybridRecommender                │   │
│   │ · ColdStartCluster         │  │ · MetricsCalculator                │   │
│   └─────────────┬──────────────┘  └────────────────────────────────────┘   │
│                 │  消费                                                       │
└─────────────────┼───────────────────────────────────────────────────────────┘
                  │  Kafka TCP
┌─────────────────▼───────────────────────────────────────────────────────────┐
│                      🔴 消息层 (Message Layer)                                │
│                         Kafka 3.x 消息队列                                   │
│   ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────┐     │
│   │ user_behavior     │  │ content_metadata  │  │ user_register        │     │
│   └──────────────────┘  └──────────────────┘  └──────────────────────┘     │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 层级颜色约定

| 颜色 | 层级 | 技术组件 | 说明 |
|------|------|---------|------|
| 🔴 | 消息层 | Kafka | 数据入口，异步解耦 |
| 💛🟠 | 计算层 | Spark Streaming / SparkSQL | 核心计算引擎 |
| 🟢 | 存储层 | MySQL | 持久化存储 |
| 🔵 | 服务层 | Flask | 业务逻辑与 API |
| 🟣 | 展示层 | Vue | 用户界面 |

### 2.3 模块依赖矩阵

|  | Kafka | Spark Streaming | SparkSQL | MySQL | Flask | Vue |
|--|-------|----------------|----------|-------|-------|-----|
| **Kafka** | — | ✅ 生产消费 | ❌ | ❌ | ❌ | ❌ |
| **Spark Streaming** | ✅ 消费 | — | ❌ | ✅ 写入 | ❌ | ❌ |
| **SparkSQL** | ❌ | ❌ | — | ✅ 读写 | ❌ | ❌ |
| **MySQL** | ❌ | ✅ 被写入 | ✅ 被读写 | — | ✅ 被读写 | ❌ |
| **Flask** | ❌ | ❌ | ❌ | ✅ 读写 | — | ✅ 被调用 |
| **Vue** | ❌ | ❌ | ❌ | ❌ | ✅ 调用 | — |

### 2.4 Cluster 部署拓扑

```
┌──────────────────────────────────────────────────────────────────┐
│                         Nginx :443 (TLS 终结)                     │
│                     /api/* → Flask 集群     / → Vue 静态资源       │
└────────────┬─────────────────────────────────┬───────────────────┘
             │                                 │
    ┌────────▼──────────┐              ┌───────▼────────┐
    │ Flask Node 1      │              │  CDN / Nginx   │
    │ 0.0.0.0:5001      │              │  Vue SPA dist  │
    └────────┬──────────┘              └────────────────┘
             │
    ┌────────▼──────────┐
    │ Flask Node 2      │
    │ 0.0.0.0:5002      │
    └────────┬──────────┘
             │
    ┌────────▼──────────┐      ┌─────────────────────┐
    │ MySQL Master      │◄─────│ MySQL Slave         │
    │ 0.0.0.0:3306      │ 复制  │ 0.0.0.0:3307        │
    └────────┬──────────┘      └─────────────────────┘
             │
    ┌────────▼──────────┐      ┌─────────────────────┐
    │ Spark Job Server  │      │ Kafka Broker :9092   │
    │ Streaming + Batch │      │ 3 Topics / 8 分区    │
    └───────────────────┘      └─────────────────────┘
```

---

## 3. 数据流详细设计

### 3.1 实时数据流（在线链路）

```
时间线 ──────────────────────────────────────────────────────────────────────►

T+0ms    T+100ms         T+500ms              T+3s                   T+3.5s
[用户]    [上游CMS]       [Kafka]              [Spark Streaming]      [Flask→Vue]
 │          │               │                      │                     │
 │ 点击播放  │               │                      │                     │
 ├─────────►│               │                      │                     │
 │          │ 写 Topic       │                      │                     │
 │          ├──────────────►│                      │                     │
 │          │               │ consumer.poll(500ms) │                     │
 │          │               ├─────────────────────►│                     │
 │          │               │                      │ DataValidator       │
 │          │               │                      │ → UserProfileAgg    │
 │          │               │                      │ → ContentHotStats   │
 │          │               │                      │ → MySQL batchInsert  │
 │          │               │                      ├────────────────────►│
 │          │               │                      │                     │ MySQL 实时表
 │          │               │                      │                     │ rt_user_profile
 │          │               │                      │                     │ rt_content_hot
 │          │               │                      │                     │   │
 │          │               │                      │                     │   │ WebSocket
 │          │               │                      │                     │   │ push
 │          │               │                      │                     │   ├──► DashboardPage
 │          │               │                      │                     │   │    指标卡片刷新
 │          │               │                      │                     │   │
 │          │               │                      │                     │   └──► EventLogPusher
 │          │               │                      │                     │        滚动日志更新
```

### 3.2 离线数据流（批处理链路）

```
Cron 触发 (每 6 小时 / 每日凌晨)
│
├─ T+0min ── SparkSQL Job 启动
│   │
│   ├── MySQLSparkReader 读取 rt_user_profile (全量)
│   ├── MySQLSparkReader 读取 rt_content_hot (近 24h)
│   ├── MySQLSparkReader 读取 rt_coldstart_cluster (全量)
│   │
│   ├── T+30min ── UserPortraitBuilder 完成
│   │   └── 写入 offline_user_portrait
│   │
│   ├── T+45min ── SimilarityMatrix 完成
│   │   └── 写入 offline_content_sim
│   │
│   ├── T+75min ── HybridRecommender 完成
│   │   └── 写入 offline_recommendations
│   │   └── 传递推荐日志 → MetricsCalculator
│   │
│   └── T+90min ── MetricsCalculator 完成
│       └── 写入 offline_metrics
│
└─ T+120min ── Job 结束，日志归档
```

---

## 4. 🗄️ 数据库详细设计

### 4.1 命名规范

| 前缀 | 含义 | 读写特性 | 数据生命周期 |
|------|------|---------|-------------|
| `rt_` | Realtime 实时表 | 高频写（Spark Streaming batch insert） | 保留 7 天，定时清理 |
| `offline_` | Offline 离线表 | 批量写（SparkSQL），频繁读（Flask API） | 保留 90 天 |
| `sys_` | System 系统表 | 少量读写 | 永久保留 |

### 4.2 实时表 DDL

#### 📊 `rt_user_profile` — 用户实时行为画像

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键 |
| `user_id` | BIGINT UNSIGNED | NOT NULL, INDEX | 用户 ID |
| `window_start` | DATETIME(3) | NOT NULL | 聚合窗口起始 |
| `window_end` | DATETIME(3) | NOT NULL | 聚合窗口结束 |
| `play_count` | INT UNSIGNED | DEFAULT 0 | 播放次数 |
| `completion_rate` | DECIMAL(5,4) | DEFAULT 0.0000 | 完播率 |
| `like_rate` | DECIMAL(5,4) | DEFAULT 0.0000 | 点赞率 |
| `favorite_rate` | DECIMAL(5,4) | DEFAULT 0.0000 | 收藏率 |
| `skip_rate` | DECIMAL(5,4) | DEFAULT 0.0000 | 跳过率 |
| `share_count` | INT UNSIGNED | DEFAULT 0 | 分享次数 |
| `comment_count` | INT UNSIGNED | DEFAULT 0 | 评论次数 |
| `preference_distribution` | JSON | NULL | 偏好风格/标签分布 `{"pop":0.35,"rock":0.20,...}` |
| `active_hours` | JSON | NULL | 活跃时段分布 `{"0":2,"1":0,...,"23":5}` |
| `is_cold_start` | TINYINT(1) | DEFAULT 1 | 是否冷启动用户 |
| `behavior_count` | INT UNSIGNED | DEFAULT 0 | 累计行为数 |
| `content_type_ratio` | JSON | NULL | 内容类型偏好比 `{"music":0.6,"video":0.4}` |
| `extended_attrs` | JSON | NULL | 扩展字段（预留） |
| `created_at` | DATETIME(3) | DEFAULT CURRENT_TIMESTAMP(3) | 创建时间 |

```sql
CREATE TABLE rt_user_profile (
    id           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    user_id      BIGINT UNSIGNED NOT NULL,
    window_start DATETIME(3) NOT NULL,
    window_end   DATETIME(3) NOT NULL,
    play_count   INT UNSIGNED DEFAULT 0,
    completion_rate DECIMAL(5,4) DEFAULT 0.0000,
    like_rate    DECIMAL(5,4) DEFAULT 0.0000,
    favorite_rate DECIMAL(5,4) DEFAULT 0.0000,
    skip_rate    DECIMAL(5,4) DEFAULT 0.0000,
    share_count  INT UNSIGNED DEFAULT 0,
    comment_count INT UNSIGNED DEFAULT 0,
    preference_distribution JSON DEFAULT NULL,
    active_hours JSON DEFAULT NULL,
    is_cold_start TINYINT(1) DEFAULT 1,
    behavior_count INT UNSIGNED DEFAULT 0,
    content_type_ratio JSON DEFAULT NULL,
    extended_attrs JSON DEFAULT NULL,
    created_at   DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    INDEX idx_user_id (user_id),
    INDEX idx_window_start (window_start),
    INDEX idx_is_cold_start (is_cold_start)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 🔥 `rt_content_hot` — 内容实时热度

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键 |
| `content_id` | BIGINT UNSIGNED | NOT NULL | 内容 ID |
| `content_type` | ENUM('music','video') | NOT NULL | 内容类型 |
| `play_count` | BIGINT UNSIGNED | DEFAULT 0 | 播放次数 |
| `like_count` | BIGINT UNSIGNED | DEFAULT 0 | 点赞数 |
| `favorite_count` | BIGINT UNSIGNED | DEFAULT 0 | 收藏数 |
| `share_count` | BIGINT UNSIGNED | DEFAULT 0 | 分享数 |
| `completion_rate` | DECIMAL(5,4) | DEFAULT 0.0000 | 完播率 |
| `interaction_rate` | DECIMAL(5,4) | DEFAULT 0.0000 | 互动率 (点赞+收藏+评论+分享)/播放 |
| `hot_score` | DECIMAL(10,4) | DEFAULT 0.0000, INDEX | 热度加权分 |
| `window_start` | DATETIME(3) | NOT NULL | 统计窗口起始 |
| `window_end` | DATETIME(3) | NOT NULL | 统计窗口结束 |
| `extended_attrs` | JSON | NULL | 扩展字段 |
| `created_at` | DATETIME(3) | DEFAULT CURRENT_TIMESTAMP(3) | 创建时间 |

```sql
CREATE TABLE rt_content_hot (
    id            BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    content_id    BIGINT UNSIGNED NOT NULL,
    content_type  ENUM('music','video') NOT NULL,
    play_count    BIGINT UNSIGNED DEFAULT 0,
    like_count    BIGINT UNSIGNED DEFAULT 0,
    favorite_count BIGINT UNSIGNED DEFAULT 0,
    share_count   BIGINT UNSIGNED DEFAULT 0,
    completion_rate DECIMAL(5,4) DEFAULT 0.0000,
    interaction_rate DECIMAL(5,4) DEFAULT 0.0000,
    hot_score     DECIMAL(10,4) DEFAULT 0.0000,
    window_start  DATETIME(3) NOT NULL,
    window_end    DATETIME(3) NOT NULL,
    extended_attrs JSON DEFAULT NULL,
    created_at    DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    INDEX idx_content (content_id, content_type),
    INDEX idx_hot_score (hot_score DESC),
    INDEX idx_window (window_start, window_end)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 🧬 `rt_coldstart_cluster` — 冷启动用户聚类结果

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键 |
| `user_id` | BIGINT UNSIGNED | NOT NULL, UNIQUE | 用户 ID |
| `cluster_id` | TINYINT UNSIGNED | NOT NULL, INDEX | 聚类簇 ID (0~K-1) |
| `cluster_center` | JSON | NOT NULL | 簇中心向量 JSON |
| `cluster_size` | INT UNSIGNED | DEFAULT 0 | 当前簇中用户数 |
| `device_type` | VARCHAR(32) | NULL | 设备类型 |
| `register_channel` | VARCHAR(64) | NULL | 注册渠道 |
| `interest_tags` | JSON | NULL | 初始兴趣标签 |
| `compute_time` | DATETIME(3) | NOT NULL | 聚类计算时间 |
| `extended_attrs` | JSON | NULL | 扩展字段 |
| `created_at` | DATETIME(3) | DEFAULT CURRENT_TIMESTAMP(3) | 创建时间 |

```sql
CREATE TABLE rt_coldstart_cluster (
    id               BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    user_id          BIGINT UNSIGNED NOT NULL,
    cluster_id       TINYINT UNSIGNED NOT NULL,
    cluster_center   JSON NOT NULL,
    cluster_size     INT UNSIGNED DEFAULT 0,
    device_type      VARCHAR(32) DEFAULT NULL,
    register_channel VARCHAR(64) DEFAULT NULL,
    interest_tags    JSON DEFAULT NULL,
    compute_time     DATETIME(3) NOT NULL,
    extended_attrs   JSON DEFAULT NULL,
    created_at       DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    UNIQUE KEY uk_user_id (user_id),
    INDEX idx_cluster_id (cluster_id),
    INDEX idx_compute_time (compute_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.3 离线表 DDL

#### 🎯 `offline_user_portrait` — 用户全量画像

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键 |
| `user_id` | BIGINT UNSIGNED | NOT NULL, UNIQUE | 用户 ID |
| `long_term_tags` | JSON | NULL | TF-IDF 加权长期兴趣标签 |
| `preference_vector` | JSON | NOT NULL | 128 维偏好向量 |
| `lifecycle_stage` | ENUM('new','active','silent','churned') | NOT NULL | 用户生命周期阶段 |
| `total_behaviors` | INT UNSIGNED | DEFAULT 0 | 累计行为总数 |
| `avg_session_duration` | DECIMAL(10,2) | DEFAULT 0.00 | 平均会话时长(秒) |
| `active_days_last_30` | TINYINT UNSIGNED | DEFAULT 0 | 近 30 天活跃天数 |
| `last_active_time` | DATETIME(3) | NULL | 最后活跃时间 |
| `favorite_content_type` | ENUM('music','video','mixed') | DEFAULT 'mixed' | 偏好内容类型 |
| `cluster_id` | TINYINT UNSIGNED | NULL | 关联的冷启动聚类 ID |
| `tag_version` | VARCHAR(32) | NOT NULL | 标签体系版本号 |
| `compute_time` | DATETIME(3) | NOT NULL | 画像计算时间 |
| `extended_attrs` | JSON | NULL | 扩展字段 |
| `created_at` | DATETIME(3) | DEFAULT CURRENT_TIMESTAMP(3) | 创建时间 |
| `updated_at` | DATETIME(3) | DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE | 更新时间 |

```sql
CREATE TABLE offline_user_portrait (
    id                  BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    user_id             BIGINT UNSIGNED NOT NULL,
    long_term_tags      JSON DEFAULT NULL,
    preference_vector   JSON NOT NULL,
    lifecycle_stage     ENUM('new','active','silent','churned') NOT NULL DEFAULT 'new',
    total_behaviors     INT UNSIGNED DEFAULT 0,
    avg_session_duration DECIMAL(10,2) DEFAULT 0.00,
    active_days_last_30 TINYINT UNSIGNED DEFAULT 0,
    last_active_time    DATETIME(3) DEFAULT NULL,
    favorite_content_type ENUM('music','video','mixed') DEFAULT 'mixed',
    cluster_id          TINYINT UNSIGNED DEFAULT NULL,
    tag_version         VARCHAR(32) NOT NULL DEFAULT 'v1.0',
    compute_time        DATETIME(3) NOT NULL,
    extended_attrs      JSON DEFAULT NULL,
    created_at          DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at          DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    UNIQUE KEY uk_user_id (user_id),
    INDEX idx_lifecycle (lifecycle_stage),
    INDEX idx_favorite_type (favorite_content_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 🔗 `offline_content_sim` — 内容相似度矩阵

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键 |
| `content_id_a` | BIGINT UNSIGNED | NOT NULL, INDEX | 内容 A 的 ID |
| `content_id_b` | BIGINT UNSIGNED | NOT NULL, INDEX | 内容 B 的 ID |
| `content_type` | ENUM('music','video') | NOT NULL | 内容类型 |
| `similarity` | DECIMAL(6,5) | NOT NULL, INDEX | 余弦相似度 [0.00000, 1.00000] |
| `sim_dimensions` | JSON | NULL | 各维度分项相似度 |
| `compute_time` | DATETIME(3) | NOT NULL | 计算时间 |
| `created_at` | DATETIME(3) | DEFAULT CURRENT_TIMESTAMP(3) | 创建时间 |

```sql
CREATE TABLE offline_content_sim (
    id             BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    content_id_a   BIGINT UNSIGNED NOT NULL,
    content_id_b   BIGINT UNSIGNED NOT NULL,
    content_type   ENUM('music','video') NOT NULL,
    similarity     DECIMAL(6,5) NOT NULL,
    sim_dimensions JSON DEFAULT NULL,
    compute_time   DATETIME(3) NOT NULL,
    created_at     DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    INDEX idx_a_type_sim (content_id_a, content_type, similarity DESC),
    INDEX idx_b_type (content_id_b, content_type),
    UNIQUE KEY uk_pair (content_id_a, content_id_b, content_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 🎁 `offline_recommendations` — 离线推荐结果

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键 |
| `user_id` | BIGINT UNSIGNED | NOT NULL, INDEX | 用户 ID |
| `content_id` | BIGINT UNSIGNED | NOT NULL | 推荐内容 ID |
| `content_type` | ENUM('music','video') | NOT NULL | 内容类型 |
| `rank` | TINYINT UNSIGNED | NOT NULL | 推荐排序位置 (1~N) |
| `score` | DECIMAL(10,6) | NOT NULL | 推荐分数 |
| `strategy` | VARCHAR(32) | NOT NULL | 推荐策略标识 |
| `reason` | VARCHAR(128) | NULL | 推荐理由（可解释性） |
| `batch_id` | VARCHAR(64) | NOT NULL | 批次标识 |
| `compute_time` | DATETIME(3) | NOT NULL | 计算时间 |
| `expire_time` | DATETIME(3) | NOT NULL | 过期时间 |
| `extended_attrs` | JSON | NULL | 扩展字段 |
| `created_at` | DATETIME(3) | DEFAULT CURRENT_TIMESTAMP(3) | 创建时间 |

```sql
CREATE TABLE offline_recommendations (
    id            BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    user_id       BIGINT UNSIGNED NOT NULL,
    content_id    BIGINT UNSIGNED NOT NULL,
    content_type  ENUM('music','video') NOT NULL,
    `rank`        TINYINT UNSIGNED NOT NULL,
    score         DECIMAL(10,6) NOT NULL,
    strategy      VARCHAR(32) NOT NULL,
    reason        VARCHAR(128) DEFAULT NULL,
    batch_id      VARCHAR(64) NOT NULL,
    compute_time  DATETIME(3) NOT NULL,
    expire_time   DATETIME(3) NOT NULL,
    extended_attrs JSON DEFAULT NULL,
    created_at    DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    INDEX idx_user_rank (user_id, `rank`),
    INDEX idx_batch (batch_id),
    INDEX idx_expire (expire_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 📈 `offline_metrics` — 推荐效果指标

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 主键 |
| `metric_date` | DATE | NOT NULL | 统计日期 |
| `user_group` | ENUM('coldstart','existing','all') | NOT NULL | 用户群 |
| `content_type` | ENUM('music','video','mixed','all') | NOT NULL | 内容类型 |
| `ctr` | DECIMAL(6,4) | DEFAULT NULL | 点击率 |
| `cvr` | DECIMAL(6,4) | DEFAULT NULL | 转化率 |
| `avg_watch_duration` | DECIMAL(10,2) | DEFAULT NULL | 人均播放时长(秒) |
| `avg_interactions` | DECIMAL(6,2) | DEFAULT NULL | 人均互动次数 |
| `coverage` | DECIMAL(6,4) | DEFAULT NULL | 推荐覆盖率 |
| `diversity` | DECIMAL(6,4) | DEFAULT NULL | 推荐多样性 |
| `coldstart_conversion` | DECIMAL(6,4) | DEFAULT NULL | 冷启动转化率（仅 coldstart 组） |
| `total_impressions` | BIGINT UNSIGNED | DEFAULT 0 | 曝光总数 |
| `total_clicks` | BIGINT UNSIGNED | DEFAULT 0 | 点击总数 |
| `total_users` | INT UNSIGNED | DEFAULT 0 | 去重用户数 |
| `compute_time` | DATETIME(3) | NOT NULL | 计算时间 |
| `created_at` | DATETIME(3) | DEFAULT CURRENT_TIMESTAMP(3) | 创建时间 |

```sql
CREATE TABLE offline_metrics (
    id                 BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    metric_date        DATE NOT NULL,
    user_group         ENUM('coldstart','existing','all') NOT NULL,
    content_type       ENUM('music','video','mixed','all') NOT NULL,
    ctr                DECIMAL(6,4) DEFAULT NULL,
    cvr                DECIMAL(6,4) DEFAULT NULL,
    avg_watch_duration DECIMAL(10,2) DEFAULT NULL,
    avg_interactions   DECIMAL(6,2) DEFAULT NULL,
    coverage           DECIMAL(6,4) DEFAULT NULL,
    diversity          DECIMAL(6,4) DEFAULT NULL,
    coldstart_conversion DECIMAL(6,4) DEFAULT NULL,
    total_impressions  BIGINT UNSIGNED DEFAULT 0,
    total_clicks       BIGINT UNSIGNED DEFAULT 0,
    total_users        INT UNSIGNED DEFAULT 0,
    compute_time       DATETIME(3) NOT NULL,
    created_at         DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    UNIQUE KEY uk_date_group_type (metric_date, user_group, content_type),
    INDEX idx_metric_date (metric_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.4 系统表 DDL

#### 👤 `sys_user` — 系统用户

```sql
CREATE TABLE sys_user (
    id            INT UNSIGNED NOT NULL AUTO_INCREMENT,
    username      VARCHAR(64) NOT NULL,
    email         VARCHAR(128) NOT NULL,
    password_hash VARCHAR(256) NOT NULL COMMENT 'bcrypt hash',
    is_active     TINYINT(1) NOT NULL DEFAULT 1,
    is_verified   TINYINT(1) NOT NULL DEFAULT 0,
    last_login_at DATETIME(3) DEFAULT NULL,
    created_at    DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at    DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    UNIQUE KEY uk_username (username),
    UNIQUE KEY uk_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 🛡️ `sys_role` — 角色定义

```sql
CREATE TABLE sys_role (
    id          TINYINT UNSIGNED NOT NULL AUTO_INCREMENT,
    name        VARCHAR(32) NOT NULL,
    description VARCHAR(128) DEFAULT NULL,
    created_at  DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    UNIQUE KEY uk_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 初始数据
INSERT INTO sys_role (name, description) VALUES
('end_user', '终端用户 — 消费推荐内容'),
('operator', '运营人员 — 查看数据大屏，查询分析结果，导出报表'),
('admin', '系统管理员 — 用户管理，权限分配，系统配置');
```

#### 🔗 `sys_user_role` — 用户-角色关联

```sql
CREATE TABLE sys_user_role (
    id       INT UNSIGNED NOT NULL AUTO_INCREMENT,
    user_id  INT UNSIGNED NOT NULL,
    role_id  TINYINT UNSIGNED NOT NULL,
    assigned_by INT UNSIGNED DEFAULT NULL COMMENT '分配者 user_id',
    assigned_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    UNIQUE KEY uk_user_role (user_id, role_id),
    INDEX idx_user_id (user_id),
    FOREIGN KEY (user_id) REFERENCES sys_user(id) ON DELETE CASCADE,
    FOREIGN KEY (role_id) REFERENCES sys_role(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 📝 `sys_audit_log` — 操作审计日志

```sql
CREATE TABLE sys_audit_log (
    id          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    operator_id INT UNSIGNED NOT NULL,
    action      VARCHAR(64) NOT NULL COMMENT '操作类型: user.create, user.disable, role.assign, config.update',
    target      VARCHAR(128) NOT NULL COMMENT '操作对象: user:123, role:2',
    detail      JSON DEFAULT NULL COMMENT '操作详情 JSON',
    ip_address  VARCHAR(45) DEFAULT NULL,
    created_at  DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    INDEX idx_operator_time (operator_id, created_at DESC),
    INDEX idx_action (action),
    FOREIGN KEY (operator_id) REFERENCES sys_user(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.5 索引策略

| 表名 | 查询模式 | 索引策略 |
|------|---------|---------|
| `rt_user_profile` | `WHERE user_id = ? AND window_start > ?` | `idx_user_id` + `idx_window_start` |
| `rt_content_hot` | `ORDER BY hot_score DESC LIMIT ?` | `idx_hot_score DESC` 覆盖排序 |
| `rt_coldstart_cluster` | `WHERE cluster_id = ?` | `idx_cluster_id` 分簇查询 |
| `offline_recommendations` | `WHERE user_id = ? ORDER BY rank` | `idx_user_rank(user_id, rank)` 联合索引 |
| `offline_metrics` | `WHERE metric_date BETWEEN ? AND ?` | `idx_metric_date` 日期范围扫描 |
| `offline_content_sim` | `WHERE content_id_a = ? AND similarity > ?` | `idx_a_type_sim` 覆盖 Top-K 相似查询 |

### 4.6 数据清理策略

```sql
-- 实时表：保留 7 天
-- 定时任务 (每日 03:00)
DELETE FROM rt_user_profile WHERE created_at < DATE_SUB(NOW(), INTERVAL 7 DAY);
DELETE FROM rt_content_hot    WHERE created_at < DATE_SUB(NOW(), INTERVAL 7 DAY);

-- 离线推荐表：保留最近 3 个批次
DELETE FROM offline_recommendations
WHERE batch_id NOT IN (
    SELECT DISTINCT batch_id FROM (
        SELECT batch_id FROM offline_recommendations
        GROUP BY batch_id ORDER BY MAX(compute_time) DESC LIMIT 3
    ) AS recent
);

-- 审计日志：保留 180 天
DELETE FROM sys_audit_log WHERE created_at < DATE_SUB(NOW(), INTERVAL 180 DAY);
```

---

## 5. 📨 Kafka 消息队列详细设计

### 5.1 Topic 定义

| Topic 名称 | 分区数 | 副本数 | 消息保留 | 压缩策略 | 说明 |
|-----------|--------|--------|---------|---------|------|
| `user_behavior` | 3 | 2 | 24h | `snappy` | 用户行为日志，吞吐量最高 |
| `content_metadata` | 2 | 2 | 72h | `snappy` | 内容元数据，更新频率低 |
| `user_register` | 2 | 2 | 72h | `snappy` | 注册事件，数据量较小 |

### 5.2 消息 Schema

#### `user_behavior` — 用户行为消息

```json
{
  "event_id": "evt_20260529_a1b2c3d4",
  "user_id": 1002345,
  "content_id": 5000123,
  "content_type": "music",
  "event_type": "play",
  "event_time": "2026-05-29T14:32:15.123Z",
  "duration": 180.5,
  "progress": 0.75,
  "device_type": "android",
  "os_version": "14.0",
  "app_version": "3.2.1",
  "channel": "organic",
  "session_id": "sess_x9y8z7",
  "region": "CN-BJ",
  "source": "recommendation_feed",
  "source_strategy": "coldstart_cluster",
  "source_rank": 3,
  "extra": {
    "network": "wifi",
    "quality": "high"
  }
}
```

**字段校验规则：**

| 字段 | 必填 | 校验规则 | 失败处理 |
|------|------|---------|---------|
| `event_id` | ✅ | 非空字符串, 最长 64 | → DLQ |
| `user_id` | ✅ | 正整数 > 0 | → DLQ |
| `content_id` | ✅ | 正整数 > 0 | → DLQ |
| `event_type` | ✅ | 枚举: play/like/favorite/comment/skip/complete/share | → DLQ |
| `event_time` | ✅ | ISO 8601 格式, 不超过当前时间 ±5min | → DLQ |
| `content_type` | ❌ | 枚举: music/video, 默认 music | 设为默认值 |
| `duration` | ❌ | 正浮点数, 默认 0.0 | 设为 0.0 |

#### `content_metadata` — 内容元数据消息

```json
{
  "content_id": 5000123,
  "content_type": "music",
  "title": "示例歌曲名",
  "artist_or_author": "示例歌手",
  "style_or_category": "pop",
  "tags": ["华语", "流行", "2024"],
  "duration": 240.0,
  "language": "zh-CN",
  "bpm": 120.5,
  "release_date": "2024-03-15",
  "action": "create"
}
```

#### `user_register` — 用户注册消息

```json
{
  "user_id": 1002345,
  "register_time": "2026-05-29T14:30:00.000Z",
  "device_type": "android",
  "os_version": "14.0",
  "register_channel": "app_store_search",
  "interest_tags": ["pop", "rock", "gaming"],
  "region": "CN-BJ",
  "age_group": "18-24",
  "gender": "unknown"
}
```

### 5.3 死信队列 (DLQ) 设计

```
Topic: user_behavior_dlq
分区: 1
保留: 7 天

消息格式 (在原始消息基础上包装):
{
  "original_message": "{...原始 JSON...}",
  "error_reason": "missing_field:user_id",
  "error_time": "2026-05-29T14:32:16.000Z",
  "source_topic": "user_behavior",
  "source_partition": 1,
  "source_offset": 12345
}
```

---

## 6. ⚡ Spark Streaming 实时分析详细设计

### 6.1 应用入口配置

```scala
// RealTimeAnalysisApp.scala
object RealTimeAnalysisApp {
  def main(args: Array[String]): Unit = {
    val conf = Map(
      "spark.app.name"                -> "HybridRecSys-Streaming",
      "spark.master"                  -> "local[*]",
      "spark.streaming.batchDuration" -> "10",
      "spark.sql.shuffle.partitions"  -> "8",
      "spark.streaming.backpressure.enabled" -> "true",
      "spark.streaming.kafka.maxRatePerPartition" -> "500",
      "spark.streaming.stopGracefullyOnShutdown" -> "true"
    )

    val spark = SparkSession.builder()
      .appName(conf("spark.app.name"))
      .master(conf("spark.master"))
      .config("spark.sql.shuffle.partitions", conf("spark.sql.shuffle.partitions"))
      .getOrCreate()

    val ssc = new StreamingContext(spark.sparkContext, Seconds(10))
    ssc.checkpoint("hdfs://namenode:9000/checkpoints/streaming")

    // Kafka 配置
    val kafkaParams = Map[String, Object](
      "bootstrap.servers"  -> "localhost:9092",
      "key.deserializer"   -> classOf[StringDeserializer],
      "value.deserializer" -> classOf[StringDeserializer],
      "group.id"           -> "hybrid_rec_streaming",
      "auto.offset.reset"  -> "latest",
      "enable.auto.commit" -> "false"
    )

    val topics = Array("user_behavior", "content_metadata", "user_register")

    // 创建 DStream
    val kafkaStream = KafkaUtils.createDirectStream[String, String](
      ssc,
      PreferConsistent,
      Subscribe[String, String](topics, kafkaParams)
    )

    // 分流
    val behaviorStream  = kafkaStream.filter(_.topic() == "user_behavior").map(_.value())
    val metadataStream  = kafkaStream.filter(_.topic() == "content_metadata").map(_.value())
    val registerStream  = kafkaStream.filter(_.topic() == "user_register").map(_.value())

    // ── 管线组装 ──
    val validator = new DataValidator()

    // 校验三条流
    val validatedBehavior = behaviorStream.transform(rdd => validator.validateBehaviorRDD(rdd))
    val validatedRegister = registerStream.transform(rdd => validator.validateRegisterRDD(rdd))

    // 用户画像聚合 (5 min 滑动窗口, 2.5 min 滑动步长)
    val profileAgg = new UserProfileAgg(
      windowDuration = Minutes(5),
      slideDuration  = Minutes(2).+(Seconds(30)),
      coldStartThreshold = 50
    )
    val userProfileRDD = profileAgg.aggregate(validatedBehavior)

    // 内容热度统计 (1 min 滚动窗口)
    val hotStats = new ContentHotStats(
      windowDuration = Minutes(1),
      weightPlay      = 1.0,
      weightCompletion = 1.5,
      weightInteraction = 2.0
    )
    val contentHotRDD = hotStats.computeHotness(validatedBehavior)

    // 冷启动聚类 (每批次)
    val cluster = new ColdStartCluster(k = 8, maxIterations = 20)
    val clusterResultRDD = cluster.cluster(validatedRegister)

    // 批量写入 MySQL
    val writer = new MySQLBatchWriter(
      batchSize = 500,
      jdbcUrl   = "jdbc:mysql://localhost:3306/hybrid_rec?useSSL=false&rewriteBatchedStatements=true"
    )

    userProfileRDD.foreachRDD(rdd => writer.writeUserProfile(rdd))
    contentHotRDD.foreachRDD(rdd => writer.writeContentHot(rdd))
    clusterResultRDD.foreachRDD(rdd => writer.writeClusterResult(rdd))

    // 启动
    ssc.start()
    ssc.awaitTermination()
  }
}
```

### 6.2 DataValidator — 数据校验器详细设计

```
┌────────────────────────────────────────────────────────────┐
│                    DataValidator                            │
├────────────────────────────────────────────────────────────┤
│ 输入: RDD[String] (原始 JSON)                               │
│                                                            │
│  ┌──────────────────────┐                                  │
│  │ 1. JSON 解析          │  json4s / Jackson               │
│  │    parse(raw)         │                                  │
│  └──────────┬───────────┘                                  │
│             │                                              │
│             ├── 解析失败 ──► sendToDLQ("parse_error")       │
│             │                                              │
│  ┌──────────▼───────────┐                                  │
│  │ 2. 必填字段校验        │  user_id, content_id,            │
│  │    checkFields()      │  event_type, event_time          │
│  └──────────┬───────────┘                                  │
│             │                                              │
│             ├── 缺失字段 ──► sendToDLQ("missing_field")     │
│             │                                              │
│  ┌──────────▼───────────┐                                  │
│  │ 3. 字段类型/范围校验    │  user_id>0, event_type 枚举     │
│  │    validateTypes()    │  event_time ±5min 窗口           │
│  └──────────┬───────────┘                                  │
│             │                                              │
│             ├── 类型错误 ──► sendToDLQ("invalid_type")      │
│             │                                              │
│  ┌──────────▼───────────┐                                  │
│  │ 4. 去重               │  key = (user_id, content_id,     │
│  │    deduplicate()      │         event_type)              │
│  │    60s 会话窗口        │  Guava Cache / Redis            │
│  └──────────┬───────────┘                                  │
│             │                                              │
│             ├── 重复记录 ──► drop (不进入 DLQ)               │
│             │                                              │
│  ┌──────────▼───────────┐                                  │
│  │ 5. 输出:               │                                  │
│  │    RDD[UserBehaviorEvent] ✓                             │
│  └──────────────────────┘                                  │
└────────────────────────────────────────────────────────────┘
```

### 6.3 UserProfileAgg — 用户画像聚合器详细设计

```
窗口: 5 min 滑动窗口, 2.5 min 滑动步长

输入: DStream[UserBehaviorEvent]
      │
      ├── map: event → (userId, event)
      │
      ├── groupByKeyAndWindow(Minutes(5), Minutes(2.5))
      │     │
      │     └── 每个用户在每个窗口内:
      │           events: Iterable[UserBehaviorEvent]
      │           │
      │           ├── calcPlayCount:         events.count(_.eventType == "play")
      │           ├── calcCompletionRate:     count("complete") / count("play")
      │           ├── calcLikeRate:           count("like") / count("play")
      │           ├── calcFavoriteRate:       count("favorite") / count("play")
      │           ├── calcSkipRate:           count("skip") / count("play")
      │           ├── calcPreferenceDistribution:
      │           │    按 content_type + style_or_category 分组计数 → 归一化
      │           ├── calcActiveHours:
      │           │    按 event_time.hour 分组计数
      │           └── isColdStart: totalBehaviorCount <= 50
      │
      └── 输出: DStream[UserProfile]
```

### 6.4 ContentHotStats — 内容热度统计算法

```scala
// 热度分数计算公式
def calcHotScore(playCount: Long, completionRate: Double, interactionRate: Double): Double = {
  // 时间衰减因子 (越新的数据权重越高)
  val timeDecay = 1.0

  weightPlay        * math.log1p(playCount) * timeDecay +
  weightCompletion   * completionRate * 100.0 +
  weightInteraction  * interactionRate * 100.0
}

// 各权重默认值:
// weightPlay        = 1.0  (播放量 — 基础热度)
// weightCompletion  = 1.5  (完播率 — 内容质量信号)
// weightInteraction = 2.0  (互动率 — 用户认可信号，权重最高)
```

### 6.5 ColdStartCluster — 冷启动聚类详细设计

```
特征编码 encodeFeatures(event: UserRegisterEvent) → Vector:
┌──────────────────┬──────────────────────────────────┐
│ 特征             │ 编码方式                          │
├──────────────────┼──────────────────────────────────┤
│ device_type      │ One-Hot (android/ios/web/other)  │
│ os_version       │ 主版本号数值化 (14.0 → 14.0)      │
│ register_channel │ One-Hot (8 个渠道)                │
│ interest_tags    │ Multi-Hot (50 个标签词表)         │
│ region           │ One-Hot (34 个省/直辖市)          │
│ age_group        │ Ordinal (6 组)                    │
└──────────────────┴──────────────────────────────────┘

输出向量维度: 4 + 1 + 8 + 50 + 34 + 6 = 103 维

训练/更新策略:
- 每批次累积新用户特征到缓冲区
- 缓冲区满 200 条或距上次训练 > 1 小时后，重新训练 KMeans
- 模型保存到 HDFS 备份
```

---

## 7. 🧠 SparkSQL 离线分析详细设计

### 7.1 调度策略

| Job | 调度频率 | Cron | 依赖 | 预估耗时 |
|-----|---------|------|------|---------|
| UserPortraitBuilder | 每 6 小时 | `0 */6 * * *` | rt_user_profile 就绪 | 30 min |
| SimilarityMatrix | 每 6 小时 | `15 */6 * * *` | UserPortraitBuilder 完成 | 15 min |
| HybridRecommender | 每 6 小时 | `45 */6 * * *` | SimilarityMatrix 完成 | 30 min |
| MetricsCalculator | 每日 02:00 | `0 2 * * *` | HybridRecommender 完成 | 15 min |

### 7.2 UserPortraitBuilder — 全量画像计算

```scala
object UserPortraitBuilder {
  def build(spark: SparkSession, batchTime: String): Unit = {

    // ── Step 1: 从 MySQL 加载数据 ──
    val profileDF = spark.read
      .format("jdbc")
      .option("url", mysqlJdbcUrl)
      .option("dbtable", "rt_user_profile")
      .option("user", "reader")
      .option("password", env("MYSQL_PASSWORD"))
      .load()

    // ── Step 2: 读取内容元数据 (用于 TF-IDF 标签计算) ──
    val metaDF = spark.read
      .format("jdbc")
      .option("dbtable", "content_metadata")
      .load()

    // ── Step 3: TF-IDF 长期兴趣标签 ──
    // 从 preference_distribution JSON 展开 + 按 user_id 聚合所有窗口
    val tagDF = profileDF
      .select($"user_id", from_json($"preference_distribution", MapType).as("tags"))
      .groupBy("user_id")
      .agg(collect_list("tags").as("tag_history"))

    // TF-IDF: 用户对标签的"文档频率"(时间窗口) × 逆用户频率
    // 结果: Map[String, Double] → JSON 写入 long_term_tags

    // ── Step 4: 128 维偏好向量 ──
    // 综合行为特征 (播放/点赞/收藏/完播) × 内容类型 (音乐/视频) × 时间衰减
    // 使用 SVD/PCA 降维到 128 维, 或直接用多层特征拼接

    // ── Step 5: 生命周期阶段判定 ──
    val lifecycleDF = profileDF.groupBy("user_id")
      .agg(
        max("behavior_count").as("total"),
        max("created_at").as("last_active"),
        count(when($"created_at" > date_sub(current_date(), 30), lit(1))).as("active_days")
      )
      .select($"user_id",
        when($"total" <= 50, lit("new"))
          .when($"active_days" >= 20, lit("active"))
          .when($"active_days" >= 5, lit("silent"))
          .otherwise(lit("churned"))
          .as("lifecycle_stage")
      )

    // ── Step 6: 写入离线表 ──
    portraitDF.write
      .mode(SaveMode.Overwrite)
      .jdbc(mysqlJdbcUrl, "offline_user_portrait", mysqlProps)
  }
}
```

### 7.3 SimilarityMatrix — 相似度矩阵计算

```
音乐特征向量:
  genre_vec    → One-Hot (30 种风格)
  language_vec → One-Hot (10 种语言)
  bpm          → MinMax 归一化 [0,1]
  duration     → MinMax 归一化 [0,1]
  tag_vec      → Multi-Hot (50 个标签)
  拼接 → ~93 维向量

视频特征向量:
  category_vec → One-Hot (20 个分类)
  language_vec → One-Hot (10 种语言)
  duration     → MinMax 归一化 [0,1]
  tag_vec      → Multi-Hot (50 个标签)
  拼接 → ~82 维向量

计算流程:
  1. 从 MySQL 加载全部音乐/视频内容特征
  2. 按 content_type 分组
  3. 每类内容内部: 计算两两余弦相似度
     sim(a,b) = dot(a,b) / (||a|| * ||b||)
  4. 只保留 sim > 0.3 的记录 (稀疏存储)
  5. 每首内容保留 Top-200 最相似项
  6. 写入 offline_content_sim
```

### 7.4 HybridRecommender — 混合推荐引擎

```
┌─────────────────────────────────────────────────────────────────┐
│                     HybridRecommender                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────── 用户分流 ───────────────┐                     │
│  │                                         │                     │
│  │  behavior_count ≤ 50 ?                  │                     │
│  │     │                    │              │                     │
│  │     YES                  NO             │                     │
│  │     │                    │              │                     │
│  │     ▼                    ▼              │                     │
│  │  ┌──────────┐    ┌──────────────┐      │                     │
│  │  │ 冷启动策略 │    │ 存量用户策略   │      │                     │
│  │  └──────────┘    └──────────────┘      │                     │
│  └─────────────────────────────────────────┘                     │
│                                                                 │
│  冷启动策略 (ColdStartStrategy):                                  │
│  ┌──────────────────────────────────────┐                       │
│  │ 1. 查询用户所在的 cluster_id          │                       │
│  │ 2. 获取同 cluster 的 Top-100 热门内容  │ ← rt_content_hot     │
│  │ 3. 基于用户 interest_tags 匹配度加分   │                       │
│  │ 4. ε-greedy Exploration (ε=0.15):     │                       │
│  │    从其他 cluster 热门中随机采样       │                       │
│  │ 5. DPP 多样性重排序                    │                       │
│  │ 6. 音乐:视频 = 6:4 比例控制           │                       │
│  │ 7. 截断 Top-50                        │                       │
│  └──────────────────────────────────────┘                       │
│                                                                 │
│  存量策略 (ExistingUserStrategy):                                 │
│  ┌──────────────────────────────────────┐                       │
│  │ 1. ALS 协同过滤:                      │                       │
│  │    用户-内容隐反馈矩阵 → 因子分解       │                       │
│  │    user_factors × item_factors → 分数 │                       │
│  │ 2. Item2Vec 内容相似:                  │                       │
│  │    基于用户最近交互的 20 条内容         │                       │
│  │    查询相似内容 (offline_content_sim)   │                       │
│  │ 3. 加权融合:                           │                       │
│  │    score = 0.5×ALS + 0.3×Item2Vec     │                       │
│  │          + 0.1×热度 + 0.1×多样性       │                       │
│  │ 4. 跨模态加权:                         │                       │
│  │    根据用户 content_type_ratio 调整     │                       │
│  │    音乐/视频比例                        │                       │
│  │ 5. 截断 Top-50                        │                       │
│  └──────────────────────────────────────┘                       │
│                                                                 │
│  ┌────────────── 过渡期平滑 ──────────────┐                     │
│  │  behavior_count ∈ [30, 80]:            │                     │
│  │    w_cold = (80 - count) / 50          │                     │
│  │    w_existing = (count - 30) / 50      │                     │
│  │    score = w_cold × cold_score          │                     │
│  │          + w_existing × existing_score  │                     │
│  └────────────────────────────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 8. 🔧 Flask 后端详细设计

### 8.1 项目结构

```
flask-backend/
├── app.py                      # 应用工厂入口
├── config.py                   # 多环境配置 (Dev/Test/Prod)
├── requirements.txt            # 依赖清单
├── extensions.py               # Flask 扩展初始化 (SQLAlchemy, JWT, CORS, SocketIO)
│
├── auth/                       # 🔐 认证模块
│   ├── __init__.py              # Blueprint 注册
│   ├── routes.py               # /api/auth/* 路由
│   └── service.py              # AuthService 业务逻辑
│
├── permissions/                # 🛡️ 权限模块
│   ├── __init__.py
│   ├── decorators.py           # @require_role(*roles) 装饰器
│   └── manager.py              # PermissionManager RBAC 逻辑
│
├── api/                        # 📡 数据查询模块
│   ├── __init__.py
│   ├── recommendations.py     # 推荐查询
│   ├── metrics.py              # 指标查询
│   ├── content.py              # 热门内容查询
│   ├── users.py                # 用户画像查询
│   └── coldstart.py            # 冷启动分析查询
│
├── export/                     # 📥 导出模块
│   ├── __init__.py
│   └── service.py              # ExportService (CSV/Excel)
│
├── websocket/                  # 📡 WebSocket 模块
│   ├── __init__.py
│   ├── dashboard.py            # DashboardPusher 大屏推送
│   └── events.py               # EventLogPusher 事件推送
│
├── email_service/              # 📨 邮件模块
│   ├── __init__.py
│   └── sender.py               # EmailService SMTP 发送
│
├── models/                     # 🗄️ ORM 模型
│   ├── __init__.py
│   ├── user.py                 # User 模型
│   ├── role.py                 # Role 模型
│   └── audit_log.py            # AuditLog 模型
│
└── utils/                      # 🔧 工具
    ├── response.py             # 统一响应格式
    ├── pagination.py           # 分页工具
    └── validators.py           # 参数校验
```

### 8.2 依赖清单

```
# requirements.txt
Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-CORS==5.0.0
Flask-SocketIO==5.4.1
Flask-JWT-Extended==4.7.1
PyMySQL==1.1.1
bcrypt==4.2.1
openpyxl==3.1.5
gevent==24.11.1
gunicorn==23.0.0
```

### 8.3 统一响应格式

```python
# utils/response.py
def success(data=None, message="ok", code=200):
    return {
        "code": code,
        "message": message,
        "data": data,
        "timestamp": datetime.utcnow().isoformat()
    }, code

def error(message="error", code=400, detail=None):
    return {
        "code": code,
        "message": message,
        "detail": detail,
        "timestamp": datetime.utcnow().isoformat()
    }, code

def paginated(items, total, page, size):
    return success({
        "items": items,
        "pagination": {
            "total": total,
            "page": page,
            "size": size,
            "pages": (total + size - 1) // size
        }
    })
```

### 8.4 🔐 认证模块接口详细设计

#### `POST /api/auth/register` — 用户注册

```
请求:
{
  "username": "zhangsan",          // 必填, 3-32 字符, 字母开头
  "email": "zhangsan@example.com", // 必填, 合法邮箱格式
  "password": "Abc@123456",        // 必填, 8-64 字符, 含大小写+数字+特殊字符
  "confirm_password": "Abc@123456" // 必填, 与 password 一致
}

成功响应 (201):
{
  "code": 201,
  "message": "注册成功，请查收验证邮件",
  "data": {
    "user_id": 1,
    "username": "zhangsan",
    "email": "zhangsan@example.com",
    "is_verified": false
  }
}

校验规则:
- username: /^[a-zA-Z][a-zA-Z0-9_]{2,31}$/
- email: 标准邮箱正则 + DNS MX 记录查询
- password: 至少 8 位, 含大写/小写/数字/特殊字符中的 3 类
- confirm_password: 严格等于 password
- 用户名/邮箱唯一性: 查 sys_user 表
```

#### `POST /api/auth/login` — 用户登录

```
请求:
{
  "account": "zhangsan",           // 用户名或邮箱
  "password": "Abc@123456"
}

成功响应 (200):
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "access_token": "eyJhbGciOi...",
    "refresh_token": "dGhpcyBpcyBh...",
    "token_type": "Bearer",
    "expires_in": 7200,
    "user": {
      "id": 1,
      "username": "zhangsan",
      "email": "zhangsan@example.com",
      "roles": ["operator"]
    }
  }
}

JWT Payload:
{
  "sub": 1,                        // user_id
  "username": "zhangsan",
  "roles": ["operator"],
  "iat": 1716983535,               // 签发时间
  "exp": 1716990735,               // 过期时间 (iat + 7200s)
  "type": "access"
}

Refresh Token:
- 随机 64 位 hex 字符串
- 存储到 sys_user 扩展字段 (或独立表)
- 有效期 7 天
- 一次性使用 (刷新后旧 token 失效)
```

### 8.5 🛡️ 权限装饰器设计

```python
# permissions/decorators.py
from functools import wraps
from flask_jwt_extended import get_jwt, verify_jwt_in_request

def require_role(*roles: str):
    """
    RBAC 角色校验装饰器
    用法: @require_role('admin', 'operator')
    """
    def wrapper(fn):
        @wraps(fn)
        def decorator(*args, **kwargs):
            verify_jwt_in_request()
            claims = get_jwt()
            user_roles = set(claims.get("roles", []))
            required = set(roles)

            if not user_roles.intersection(required):
                return error("权限不足", 403, detail={
                    "required": list(required),
                    "current": list(user_roles)
                })

            return fn(*args, **kwargs)
        return decorator
    return wrapper


# 使用示例
@query_bp.route('/api/metrics')
@require_role('admin', 'operator')
def get_metrics():
    ...
```

### 8.6 📡 查询接口详细设计

| 接口 | 方法 | 认证 | 权限 | 参数 |
|------|------|------|------|------|
| `/api/recommendations` | GET | JWT | operator, admin | `user_id` (必填), `page` (默认1), `size` (默认20) |
| `/api/metrics` | GET | JWT | operator, admin | `start_date`, `end_date`, `group_by` (user_group/content_type) |
| `/api/content/hot` | GET | JWT | operator, admin | `content_type` (music/video/all), `top_n` (默认10, 最大100) |
| `/api/users/profile` | GET | JWT | operator, admin | `user_id` (必填) |
| `/api/coldstart/analysis` | GET | JWT | operator, admin | 无 |
| `/api/admin/users` | GET | JWT | admin | `page`, `size`, `is_active` (筛选) |
| `/api/admin/users/<id>/roles` | PUT | JWT | admin | `role_ids` (数组) |
| `/api/admin/users/<id>/status` | PATCH | JWT | admin | `is_active` (bool) |
| `/api/admin/audit-logs` | GET | JWT | admin | `page`, `size`, `operator_id`, `action` |

### 8.7 📡 WebSocket 推送设计

```
端点: ws://host/ws/dashboard

服务端 → 客户端推送协议 (1 次/秒):

{
  "type": "dashboard_snapshot",
  "data": {
    "online_users": 12580,           // 📊 实时在线用户数
    "daily_recommendations": 984200, // 📊 今日推荐总数
    "ctr_current": 0.0632,           // 📊 当前 CTR
    "ctr_trend": [0.061, 0.062, ...],// 📈 最近 60 个秒级 CTR 数据点
    "avg_watch_duration": 187.5,     // 📊 人均播放时长
    "hot_content_top5": [            // 🔥 实时 Top 5
      {"content_id": 5001, "title": "...", "hot_score": 98.5, "type": "music"},
      ...
    ],
    "content_ratio": {               // 🍩 音乐vs视频占比
      "music": 0.62,
      "video": 0.38
    },
    "coldstart_stats": {             // 🧊 冷启动统计
      "new_today": 3420,
      "conversion_rate": 0.45,
      "cluster_distribution": {      // 聚类分布
        "0": 0.15, "1": 0.12, "2": 0.18, "3": 0.10,
        "4": 0.08, "5": 0.14, "6": 0.13, "7": 0.10
      }
    }
  },
  "timestamp": "2026-05-29T14:32:16.000Z"
}
```

```
端点: ws://host/ws/events

服务端 → 客户端推送协议 (有新行为时推送):

{
  "type": "user_event",
  "data": {
    "user_id": 1002345,
    "event_type": "play",
    "content_type": "music",
    "content_title": "示例歌曲名",
    "region": "CN-BJ",
    "timestamp": "2026-05-29T14:32:15.000Z"
  }
}
```

---

## 9. 🎨 Vue 前端详细设计

### 9.1 项目结构

```
vue-frontend/
├── public/
│   └── favicon.ico
├── src/
│   ├── main.js                    # 入口：创建 App + Router + Pinia
│   ├── App.vue                    # 根组件 (布局 + <router-view>)
│   │
│   ├── router/
│   │   └── index.js               # 路由配置 + beforeEach 守卫
│   │
│   ├── stores/
│   │   ├── auth.js                # Pinia AuthStore (Token/角色)
│   │   └── dashboard.js           # Pinia DashboardStore (大屏数据)
│   │
│   ├── api/
│   │   ├── index.js               # Axios 实例工厂
│   │   ├── auth.js                # 认证 API 封装
│   │   ├── query.js               # 查询 API 封装
│   │   ├── admin.js               # 管理 API 封装
│   │   └── export.js              # 导出 API 封装
│   │
│   ├── composables/
│   │   ├── useWebSocket.js        # WebSocket 连接管理 Composable
│   │   └── useECharts.js          # ECharts 初始化和 Resize Composable
│   │
│   ├── views/
│   │   ├── LoginView.vue          # 🔑 登录页
│   │   ├── RegisterView.vue       # 🔑 注册页
│   │   ├── DashboardView.vue      # 📊 数据大屏
│   │   ├── QueryView.vue          # 🔍 数据查询
│   │   ├── UserProfileDetail.vue  # 🔍 用户画像详情
│   │   └── AdminView.vue          # ⚙️ 系统管理
│   │
│   ├── components/
│   │   ├── common/
│   │   │   ├── AppHeader.vue      # 顶部导航栏
│   │   │   ├── AppSidebar.vue     # 侧边菜单
│   │   │   ├── Pagination.vue     # 分页组件
│   │   │   └── DateRangePicker.vue # 日期范围选择器
│   │   │
│   │   └── dashboard/
│   │       ├── MetricCard.vue     # 指标卡片
│   │       ├── TrendLine.vue      # 趋势折线图 (ECharts)
│   │       ├── HotRankBar.vue     # 热度排行柱状图 (ECharts)
│   │       ├── ContentRatioPie.vue # 内容占比环形图 (ECharts)
│   │       ├── ClusterPie.vue     # 聚类分布饼图 (ECharts)
│   │       ├── ConversionFunnel.vue # 转化漏斗图 (ECharts)
│   │       └── EventLogStream.vue # 实时行为滚动日志
│   │
│   ├── utils/
│   │   ├── auth.js                # Token 存取/解析工具
│   │   └── format.js              # 数字/日期格式化工具
│   │
│   └── styles/
│       ├── variables.css          # CSS 变量 (主题色)
│       └── dashboard.css          # 大屏专用样式 (1920×1080 适配)
│
├── package.json
└── vite.config.js
```

### 9.2 路由设计

```javascript
// router/index.js
const routes = [
  // ── 公开路由 ──
  { path: '/login',    name: 'Login',    component: () => import('@/views/LoginView.vue') },
  { path: '/register', name: 'Register', component: () => import('@/views/RegisterView.vue') },

  // ── 需认证路由 ──
  {
    path: '/',
    component: () => import('@/components/common/AppLayout.vue'),  // 含 Header + Sidebar
    meta: { requiresAuth: true },
    children: [
      { path: '', redirect: '/dashboard' },
      { path: 'dashboard', name: 'Dashboard', component: () => import('@/views/DashboardView.vue'),
        meta: { roles: ['operator', 'admin'] } },
      { path: 'query',     name: 'Query',     component: () => import('@/views/QueryView.vue'),
        meta: { roles: ['operator', 'admin'] } },
      { path: 'query/user/:id', name: 'UserDetail', component: () => import('@/views/UserProfileDetail.vue'),
        meta: { roles: ['operator', 'admin'] } },
      { path: 'admin',     name: 'Admin',     component: () => import('@/views/AdminView.vue'),
        meta: { roles: ['admin'] } },
    ]
  },

  // ── 兜底 ──
  { path: '/:pathMatch(.*)*', redirect: '/login' }
]

// 路由守卫
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()

  if (to.meta.requiresAuth && !authStore.isAuthenticated) {
    return next('/login')
  }

  if (to.meta.roles && !to.meta.roles.some(r => authStore.hasRole(r))) {
    return next('/dashboard')  // 无权限 → 跳转首页
  }

  next()
})
```

### 9.3 📊 数据大屏页面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│  🎵 冷启动音乐与视频混合推荐系统 — 实时数据监控大屏         2026-05-29 14:32 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │👥 在线用户 │ │📨 今日推荐 │ │📈 实时CTR │ │⏱️ 人均时长 │ │🧊 冷启动转化  │ │
│  │  12,580  │ │ 984,200  │ │  6.32%   │ │ 187.5s   │ │   45.2%      │ │
│  │  ↑ 5.2%  │ │  ↑ 8.1%  │ │ ↑ 0.3pp  │ │  ↓ 2.1s   │ │   ↑ 1.8pp   │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────────┘ │
│                                                                         │
│  ┌─────────────────────────────────┐ ┌──────────────────────────────┐  │
│  │ 📈 CTR / 播放量趋势 (24h)        │ │ 🍩 音乐 vs 视频推荐占比       │  │
│  │  ┌──────────────────────┐       │ │        ┌──────┐             │  │
│  │  │    /\      /\        │       │ │        │ 62%  │ 音乐        │  │
│  │  │   /  \    /  \  /\   │       │ │        │      │             │  │
│  │  │  /    \/\/    \/  \  │       │ │        └──────┘             │  │
│  │  │ /                    │       │ │        ┌──────┐             │  │
│  │  └──────────────────────┘       │ │        │ 38%  │ 视频        │  │
│  └─────────────────────────────────┘ │        └──────┘             │  │
│                                      └──────────────────────────────┘  │
│  ┌─────────────────────────────────┐ ┌──────────────────────────────┐  │
│  │ 🔥 内容热度 Top 10               │ │ 🧊 冷启动用户聚类分布          │  │
│  │  ┌────────────────────┐         │ │  ┌──────────────────┐       │  │
│  │  │ ████████████ 歌A   │         │ │  │  🟠 C0  ████ 15%  │       │  │
│  │  │ ██████████   歌B   │         │ │  │  🟡 C1  ███  12%  │       │  │
│  │  │ █████████    歌C   │         │ │  │  🟢 C2  █████ 18% │       │  │
│  │  │ ████████     视A   │         │ │  │  ...              │       │  │
│  │  │ ...                │         │ │  └──────────────────┘       │  │
│  │  └────────────────────┘         │                               │  │
│  └─────────────────────────────────┘ └──────────────────────────────┘  │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 📜 用户行为实时滚动日志                                            │  │
│  │ [14:32:15] 👤 user_1002345 ▶️ 播放 [音乐]《示例歌曲A》📍 CN-BJ      │  │
│  │ [14:32:14] 👤 user_0678234 ❤️ 点赞 [视频]《示例视频B》📍 CN-SH     │  │
│  │ [14:32:13] 👤 user_0456123 ⭐ 收藏 [音乐]《示例歌曲C》📍 CN-GD     │  │
│  │ ...                                                               │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 9.4 WebSocket Composable

```javascript
// composables/useWebSocket.js
import { ref, onMounted, onUnmounted } from 'vue'

export function useWebSocket(url, onMessage) {
  const isConnected = ref(false)
  let ws = null
  let reconnectTimer = null
  let pingTimer = null
  let retryCount = 0
  const maxRetryDelay = 30000  // 最大重连间隔 30s

  function connect() {
    ws = new WebSocket(url)

    ws.onopen = () => {
      isConnected.value = true
      retryCount = 0
      startPing()
    }

    ws.onmessage = (event) => {
      const payload = JSON.parse(event.data)
      onMessage(payload)
    }

    ws.onclose = () => {
      isConnected.value = false
      stopPing()
      scheduleReconnect()
    }

    ws.onerror = () => {
      ws.close()  // 触发 onclose → 重连
    }
  }

  function scheduleReconnect() {
    const delay = Math.min(1000 * Math.pow(2, retryCount), maxRetryDelay)
    retryCount++
    reconnectTimer = setTimeout(connect, delay)
  }

  function startPing() {
    pingTimer = setInterval(() => {
      if (ws.readyState === WebSocket.OPEN) {
        ws.send(JSON.stringify({ type: 'ping' }))
      }
    }, 30000)
  }

  function stopPing() {
    clearInterval(pingTimer)
  }

  function disconnect() {
    clearTimeout(reconnectTimer)
    stopPing()
    if (ws) ws.close()
  }

  onMounted(connect)
  onUnmounted(disconnect)

  return { isConnected, disconnect }
}
```

---

## 10. 📊 推荐算法详细设计

### 10.1 ALS 协同过滤 (存量用户)

```
输入: 用户-内容交互矩阵 R (隐反馈)
  - 播放 = 1.0
  - 完播 = 2.0
  - 点赞 = 3.0
  - 收藏 = 4.0
  - 分享 = 5.0
  - 跳过 = -1.0 (负反馈)
  - 无交互 = 0

参数:
  rank          = 50      // 隐因子维度
  regParam      = 0.1     // L2 正则化系数
  alpha         = 40.0    // 隐反馈置信度权重
  maxIter       = 10      // 最大迭代次数

输出:
  userFactors:  U × 50 矩阵
  itemFactors:  I × 50 矩阵

推荐分数:
  score(u,i) = dot(userFactors[u], itemFactors[i])
```

### 10.2 Item2Vec 内容向量召回

```
训练样本构建:
  - 取每个用户的交互序列 (按时间排序)
  - 滑动窗口 size=5, 生成 (content_i, content_j) 共现对
  - 音乐和视频在同一个向量空间中训练 (共享 embedding)

Word2Vec 参数:
  vectorSize = 64
  windowSize = 5
  minCount   = 3
  negative   = 5
  iterations = 10

召回:
  - 取用户最近交互的 20 条内容
  - 查询每条的 embedding
  - 取平均作为用户实时兴趣向量
  - 在内容 embedding 空间做 ANN 检索 (Top-100)
```

### 10.3 DPP 多样性重排序

```
DPP (Determinantal Point Process) 核心思想:
  在保证相关性的前提下, 最大化推荐集合的"体积"(多样性)

物品核矩阵 L:
  L[i][j] = q(i) × sim(i,j) × q(j)

  其中:
    q(i) = exp(score(i)) / Σ exp(score)  // 归一化相关性分数
    sim(i,j) = 内容相似度 (来自 offline_content_sim)

求解 (贪心近似):
  selected = []
  while len(selected) < N:
    i* = argmax_{i∉selected} log det(L[selected ∪ {i}])
    selected.append(i*)

  实际实现中, 取 Top-100 候选做 DPP 重排:
    - 音乐候选 60 条 + 视频候选 40 条 = 100 条
    - DPP 重排后取 Top-50
```

### 10.4 跨模态加权融合公式

```
最终推荐分数 (存量用户):

  final_score(u, i) =
      0.50 × ALS_score(u,i)                           // 协同过滤
    + 0.30 × Item2Vec_score(u,i)                      // 内容召回
    + 0.10 × hot_score(i)                             // 热度
    + 0.05 × (1 - avg_similarity(i, selected))        // 多样性奖励
    + 0.05 × content_type_match(u, i)                 // 类型偏好匹配

  其中 content_type_match:
    = user_content_type_ratio[content_type(i)]        // 用户的音乐/视频偏好比

冷启动推荐分数:
  final_score(u, i) =
      0.35 × cluster_hot_score(i)                     // 同簇热门
    + 0.30 × tag_match_score(u.interest_tags, i.tags) // 标签匹配
    + 0.20 × global_hot_score(i)                      // 全局热门
    + 0.15 × exploration_bonus(i)                     // ε-greedy 探索

过渡期权重平滑:
  ratio = (behavior_count - 30) / 50                  // ratio ∈ [0, 1]
  final_score = (1-ratio) × coldstart_score + ratio × existing_score
```

---

## 11. 🔐 安全设计

### 11.1 身份认证流程

```
┌──────────┐         ┌──────────┐         ┌──────────┐
│  Vue SPA  │         │  Flask    │         │  MySQL   │
└────┬─────┘         └────┬─────┘         └────┬─────┘
     │                     │                    │
     │  POST /api/auth/login                     │
     │  {account, password}                      │
     │ ──────────────────►│                     │
     │                     │  SELECT * FROM       │
     │                     │  sys_user WHERE      │
     │                     │  username=? OR       │
     │                     │  email=?             │
     │                     │ ──────────────────►│
     │                     │     user row         │
     │                     │ ◄──────────────────│
     │                     │                     │
     │                     │  bcrypt.verify(      │
     │                     │    password,         │
     │                     │    password_hash)    │
     │                     │                     │
     │                     │  若成功:              │
     │                     │  - 签发 access_token  │
     │                     │    (HS256, 2h,       │
     │                     │     sub+roles+iat+exp)
     │                     │  - 生成 refresh_token │
     │                     │    (随机 64 hex + 7d) │
     │                     │  - 更新 last_login_at │
     │                     │                     │
     │  200 {access_token,│                     │
     │       refresh_token,│                    │
     │       user}         │                     │
     │ ◄──────────────────│                     │
     │                     │                     │
     │  存储 token 到       │                     │
     │  Pinia AuthStore +   │                     │
     │  localStorage        │                     │
```

### 11.2 API 请求安全链路

```
每一次 API 请求:

  1. Vue Axios 拦截器:
     - 从 AuthStore 读取 access_token
     - 注入 Authorization: Bearer <access_token>

  2. 请求到达 Flask:
     - @jwt_required() 装饰器验证 JWT 签名 (HS256)
     - 检查 exp (过期), iat (签发时间)
     - 解析 sub, roles 到请求上下文

  3. @require_role(*roles) 装饰器:
     - 从 JWT claims 取出 roles
     - 与路由所需 role 做交集
     - 交集为空 → 403 Forbidden
     - 交集非空 → 放行

  4. CORS 校验:
     - Origin 头必须在 ALLOWED_ORIGINS 白名单内
     - 预检请求 OPTIONS 返回允许的 Method/Headers

  5. 参数化查询:
     - 所有 SQL 查询使用 SQLAlchemy ORM / 参数绑定
     - 禁止字符串拼接 SQL
```

### 11.3 密码策略

| 策略 | 要求 |
|------|------|
| 最小长度 | 8 字符 |
| 字符复杂度 | 大写字母 + 小写字母 + 数字 + 特殊字符，至少满足 3 类 |
| 哈希算法 | bcrypt (cost factor = 12) |
| 登录失败限制 | 连续 5 次失败锁定 15 分钟 |
| 密码历史 | 不允许与最近 3 次密码相同 |

---

## 12. 🚀 部署设计

### 12.1 环境规划

| 环境 | 用途 | Spark Mode | MySQL | Flask |
|------|------|-----------|-------|-------|
| 🧑‍💻 Dev | 本地开发 | `local[*]` | 单机 Docker | 单实例 debug mode |
| 🧪 Test | 集成测试 | `local[*]` / `yarn-client` | 单机 + 从库 | 双实例 + Nginx |
| 🏭 Prod | 生产环境 | `yarn-cluster` | 主从 + 读写分离 | 4 实例 + Nginx + Gunicorn |

### 12.2 Docker Compose (开发环境)

```yaml
# docker-compose.yml
version: '3.8'
services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    depends_on: [zookeeper]
    ports: ["9092:9092"]
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  mysql:
    image: mysql:8.0
    ports: ["3306:3306"]
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: hybrid_rec
    volumes:
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql
      - mysql_data:/var/lib/mysql

  flask:
    build: ./flask-backend
    ports: ["5000:5000"]
    depends_on: [mysql]
    environment:
      FLASK_ENV: development
      MYSQL_HOST: mysql
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}

  vue:
    build: ./vue-frontend
    ports: ["3000:80"]
    depends_on: [flask]

volumes:
  mysql_data:
```

### 12.3 环境变量清单

| 变量名 | 说明 | 示例值 |
|--------|------|--------|
| `KAFKA_BOOTSTRAP_SERVERS` | Kafka Broker 地址 | `localhost:9092` |
| `MYSQL_MASTER_URL` | MySQL 主库连接 | `jdbc:mysql://master:3306/hybrid_rec` |
| `MYSQL_SLAVE_URL` | MySQL 从库连接 | `jdbc:mysql://slave:3307/hybrid_rec` |
| `MYSQL_USER` | MySQL 用户名 | `hybrid_app` |
| `MYSQL_PASSWORD` | MySQL 密码 | `***` |
| `JWT_SECRET_KEY` | JWT HS256 签名密钥 | `至少 32 位随机字符串` |
| `SMTP_HOST` | 邮件服务器地址 | `smtp.example.com` |
| `SMTP_PORT` | 邮件服务器端口 | `587` |
| `SPARK_MASTER` | Spark Master 地址 | `local[*]` 或 `yarn` |
| `FLASK_ENV` | Flask 运行模式 | `production` |
| `ALLOWED_ORIGINS` | CORS 白名单 | `https://app.example.com` |

### 12.4 生产环境启动顺序

```
Phase 1 — 基础设施
  ├── 1.1 启动 Zookeeper 集群 (3 节点)
  ├── 1.2 启动 Kafka 集群 (3 Broker)
  └── 1.3 启动 MySQL 主从 (1 Master + 1 Slave)

Phase 2 — 计算层
  ├── 2.1 提交 Spark Streaming Job (yarn-cluster)
  └── 2.2 配置 Cron 调度 SparkSQL Batch Jobs

Phase 3 — 服务层
  ├── 3.1 启动 Flask 实例 (Gunicorn, 4 workers × 2 实例)
  └── 3.2 启动 Nginx (反向代理 + 静态资源)

Phase 4 — 前端
  ├── 4.1 构建 Vue SPA: npm run build
  └── 4.2 部署 dist/ 到 Nginx / CDN

Phase 5 — 验证
  ├── 5.1 健康检查: GET /api/health
  ├── 5.2 Kafka 消费延迟检查
  └── 5.3 数据大屏连通性检查
```

---

## 附录

### A. 术语对照表

| 术语 | 说明 |
|------|------|
| 冷启动 (Cold Start) | 新用户缺乏历史行为数据，推荐系统难以给出个性化推荐的问题 |
| 混合推荐 (Hybrid Recommendation) | 同时推荐音乐和视频两种内容类型，在同一个 feed 中混合展示 |
| DPP | Determinantal Point Process，行列式点过程，用于推荐列表的多样性重排序 |
| ALS | Alternating Least Squares，交替最小二乘法，Spark MLlib 中的协同过滤算法 |
| Item2Vec | 基于 Word2Vec 思想的内容向量化方法，学习内容的低维嵌入表示 |
| DLQ | Dead Letter Queue，死信队列，用于存放处理失败的消息 |
| ε-greedy | Epsilon-Greedy 探索策略，以概率 ε 随机选择以探索新可能 |
| RBAC | Role-Based Access Control，基于角色的访问控制 |
| DStream | Discretized Stream，Spark Streaming 中的离散数据流抽象 |

### B. 文档修订记录

| 版本 | 日期 | 修订内容 | 作者 |
|------|------|---------|------|
| V1.0 | 2026-05-29 | 初始版本，完整详细设计 | 架构组 |
